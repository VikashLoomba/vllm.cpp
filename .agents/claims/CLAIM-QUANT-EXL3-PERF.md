# CLAIM-QUANT-EXL3-PERF

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-QUANT-EXL3-PERF` | `QUANT-EXL3-PERF` (`ACTIVE`) | Claude Code (opus-5), fresh implementer — review goes to a different agent | isolated worktree `/home/mudler/_git/vllm.cpp-exl3perf`, based on `3d045ba1b`; the CUDA arm is built and run under an `rc` lease on `dgx:gpu0`, staged at `/workspace/exl3perf-2570` | `row/QUANT-EXL3-PERF`, issue [#2570](https://github.com/mudler/vllm.cpp/issues/2570) | Owns ONLY: `Exl3GemvArmInstantiated` and `GemvKernel` in `src/vt/cuda/cuda_exl3.cu` together with the arm-set and kernel-narrowing comments directly above them; the device case and the new envelope case in `tests/vt/test_exl3_gemv.cpp`; `.agents/specs/quant-exl3-perf.md`; the `QUANT-EXL3-PERF` matrix row and the QUANT count in `scripts/check-agent-record.py`; and this row's entry in `RUNNABLE_BASELINE` in `scripts/check-gate-commands.py`. EXCLUDES the GEMV kernel body, which stays specialized to `bits == 3` and whose `(4, 2)` widening is a kernel port left OWED; EXCLUDES `src/vt/exl3_policy.cpp`, whose envelope is upstream verbatim and unchanged; EXCLUDES the fused MoE arm, which stays `(3, 1)`; EXCLUDES the regular kernel's `Exl3ArmInstantiated` table, which `QUANT-EXL3-MUL1` owns; and EXCLUDES every codebook-0 and codebook-1 path, which must stay byte-identical | `ACTIVE` | 2026-09-02 — spec and row committed before the arm. Numeric gate and the interleaved decode A/B are QUEUED on `dgx:gpu0` and are reported as PENDING until the lease runs; a CPU preflight cannot compile `cuda_exl3.cu` at all and is never reported as a device result |
