# Vulkan — full op surface, optimized against llama.cpp Vulkan

**Rows:** `BACKEND-VULKAN` (backend-matrix), `BACKEND-GATE-VULKAN-LLAMACPP`
(backend-matrix, `INVENTORIED`), `BENCH-VK-LLAMA`
([competitive-benchmarks.md](competitive-benchmarks.md)), `ROAD-V1-D1`
(roadmap portfolio).
**Claim:** `CLAIM-VULKAN-FULL-1`.
**Predecessor spec:** [backend-fanout-metal-vulkan-xpu.md](backend-fanout-metal-vulkan-xpu.md)
— owns W0/V1 (the landed skeleton). This spec supersedes its `V2`/`V3`/`V4` work
rows and does NOT re-litigate the landed design; read § V1 landed there first.
**Upstream / port source:** llama.cpp `ggml/src/ggml-vulkan/` @ `237ad9b96`
(verified present locally, 132 `.comp` + 26 `.glsl`). **vLLM has no Vulkan path
anywhere**, so this is a recorded extension
([porting-inventory.md](../porting-inventory.md) §9), not a mirror.
**Status:** **`VK-A1` LANDED 2026-08-06** (§6.1). Everything else PLAN ONLY.

> **CORRECTION, 2026-08-06 — two baseline claims in this spec were WRONG and are
> superseded by §1.1.** This spec stated, from the backend matrix, that Vulkan's
> unregistered ops make `vt::GetOp` THROW and that its op surface is "8 of 83".
> Both are false. Accelerator-seam row `S5` (`af0b21ba`) gave unified-memory
> devices a **portable reference tier**, so a missed `GetOp` installs the CPU
> kernel instead of throwing, and Vulkan is eligible. Measured on the tree:
> **87** CPU-registered ops, **8** native on Vulkan, **79** served by the
> reference tier, **0** throwing. The op-gap arithmetic in §2 (83 − 8 = 75) is
> therefore off by the same four ops; the GROUPING there is still the work, but
> the count of record is 79, not 75. Read §1.1 before quoting any number here.

---

## 0. Honesty statement — what is and is not claimed

Nothing here is measured except the baseline in §1, which is re-derived from the
current tree rather than quoted from the predecessor spec. Specifically **not**
claimed: that Vulkan runs any model, that any speed number exists, that beating
llama.cpp Vulkan on GB10 constitutes beating llama.cpp, or that any sub-project
below is sized from experience rather than from reading the port source.

One framing correction is owed up front, because it changes what "beat
llama.cpp" is worth. **On GB10 the competitor is llama.cpp's Vulkan backend, and
llama.cpp's own CUDA backend will beat both of us there.** Vulkan on an NVIDIA
part is nobody's fastest path; it is the portability path. A win recorded here is
a win over *the Vulkan maturity floor on the box we own*, and the record must say
exactly that rather than "we beat llama.cpp". The claim that would matter to a
user — Vulkan winning where CUDA/ROCm/Metal do not exist — is `VK-I`, and it is
hardware-blocked until an RDNA or Arc board is acquired (user decision 2026-08-06:
GB10 first, acquire later).

---

## 1. Baseline — where Vulkan stands, re-measured from the tree

Landed in ONE commit, `1cb5f643` (2026-07-22), and untouched since.

| Surface | State |
|---|---|
| `vt::Backend` + registrar + allocation registry | [vulkan_backend.cpp](../../src/vt/vulkan/vulkan_backend.cpp) (150 lines) |
| dlopen entry-point loader | [vulkan_loader.cpp](../../src/vt/vulkan/vulkan_loader.cpp) (93) |
| instance/device/queue/memory/pipeline context | [vulkan_context.cpp](../../src/vt/vulkan/vulkan_context.cpp) (568) |
| op dispatch + `RegisterOp` | [vulkan_ops.cpp](../../src/vt/vulkan/vulkan_ops.cpp) (329) |
| GLSL shaders | [shaders/](../../src/vt/vulkan/shaders/) — 7 `.comp` + 1 `.glsl` |
| committed SPIR-V | [vulkan_spirv.h](../../src/vt/vulkan/vulkan_spirv.h) — 3,491 lines, 7 modules, regenerated by [gen-vulkan-spirv.py](../../scripts/gen-vulkan-spirv.py) |
| `Platform` + registrar | [platforms/vulkan.cpp](../../src/vllm/platforms/vulkan.cpp) |
| Khronos TYPE headers | [third_party/vulkan/](../../third_party/vulkan/) @ `vulkan-sdk-1.4.328.1`, `VK_NO_PROTOTYPES`, nothing linked |
| build switch | tri-state `VLLM_CPP_VULKAN`, **AUTO resolves OFF** (deliberately unlike Metal) |

**Op coverage, counted from the tree on 2026-08-06** (`RegisterOp(OpId::…)`
occurrences, deduplicated, walked recursively per backend directory):

| Backend | Registered `OpId`s |
|---|---|
| CUDA | **103** |
| CPU | **83** |
| Metal | **19** |
| **Vulkan** | **8** |

`OpId` itself has **106 entries** — the "75-entry `OpId` table" quoted in
[accelerator-seam-audit.md](accelerator-seam-audit.md) and the predecessor spec
is **stale**; the table grew. This spec uses the counted figures and every later
row must re-count rather than quote.

Registered on Vulkan: `kAdd` (incl. rank-1 bias broadcast), `kRelu`,
`kSiluAndMul`, `kCastBf16`, `kCastF32`, `kLayerNorm`, `kRmsNorm` (incl. in-place
residual stream), and ONE `kFusedChain` Tier-1 interpreter, which inherits the
whole portable fusion catalog.

**Structural limits the skeleton records about itself**, all of which cap this
campaign and none of which are defects:
- dispatch is **synchronous and mutex-serialized** — record + submit + fence-wait
  per op. Correct, explicitly not fast.
- `SupportsGraphCapture()` is **FALSE**. A pre-recorded `VkCommandBuffer` is the
  named eventual mapping.
- **no staging path** for non-host-visible memory, so a discrete GPU works but
  slowly. GB10 is unified, so this path is dead code until `VK-I`.
- `get_attn_backend_priority()` returns **EMPTY** deliberately.

**Numerics already measured** (GB10, same binary, CPU oracle vs BOTH device
backends): worst-case Vulkan NMSE `2.92e-14` against a `5e-4` bar, within 1.3x of
CUDA's own worst case on the same box; `Copy`/`Memset`/the bf16<->f32 codec
bit-exact by `memcmp`. `test_vulkan_backend` 8/8 (82/82),
`test_backend_cross_device` 5/5 (144/144). Dev-box `llvmpipe` gives a **working
GPU-free CI path**.

**Record drift to repair (owned by `VK-A1`).** `feature-matrix.md:280` carries
`BACKEND-VULKAN` as `INVENTORIED | runtime absent` while `backend-matrix.md:233`
carries it as `ACTIVE` (gated skeleton). Same row, two states. `backend-matrix`
is correct; `feature-matrix` is the stale one. **REPAIRED 2026-08-06 by `VK-A1`.**

### 1.1 What `VK-A1` MEASURED, superseding §1 and §2 above

The op surface was re-counted at RUNTIME (not by grepping `RegisterOp`), on a
Vulkan-ON build, by asking `vt::OpRegistered` and `vt::GetOp` for every `OpId`:

| | at `VK-A1` | after the first `VK-B` bricks |
|---|---|---|
| CPU-registered ops | **87** | **87** |
| **NATIVE** on Vulkan | **8** | **16** |
| served by the **portable reference tier** (S5, CPU kernel on shared memory) | **79** | **71** |
| **ABSENT** (`GetOp` throws) | **0** | **0** |

The six that moved are `kMatmul`, `kMatmulBT` (dense GEMM, both orientations),
`kEmbedding`, `kGreedyArgmax`, `kPagedAttention` and `kReshapeAndCache` — the
model's two ends, the op it spends most of its time in, and the attention block's
read and write halves.

**`VK-B`'s next brick needs a DECISION, not just work: RoPE.** The CPU kernel
computes the angle in DOUBLE (`cpu_ops.cpp:701-705` — `std::pow` for the
frequency, `std::cos`/`std::sin` of `pos * freq`), and an f32 transcription loses
precision badly at long context, where `pos` is in the thousands. Two ways out:
require `shaderFloat64` and transcribe faithfully, or implement `kRopeFromCache`
(the apply, pure f32 multiply-add) natively and leave `kRopeCosSinCache` (the
once-per-model table build, where the double math lives) on the reference tier.
The second is what vLLM's own structure suggests and costs no device feature. Re-measure with the same method rather than quoting this table.

`vt::ReferenceTierEligible(kVULKAN)` is TRUE — GB10 integrated and llvmpipe both
report unified memory. So **every op the CPU backend has is already reachable on
Vulkan**, and the campaign's real content is moving those 79 off the host, which
is a PERFORMANCE project rather than a make-it-run project. `vt::GetReferenceTierHits()`
is the built-in progress metric: it must reach **0** for a genuinely native run.

**One honest limit.** Op resolution is not an end-to-end claim.
`VulkanPlatform::get_attn_backend_priority()` is still deliberately EMPTY, so the
attention-backend registry has no Vulkan entry — a separate gate at the platform
seam that `VK-A1` did not test past. No model has been run on Vulkan.

**Why nobody knew.** `test_vulkan_backend`'s "unimplemented ops throw" assertion
had been RED since `S5` landed. The Metal sibling was updated as Metal work
continued; Vulkan was not, because **`VLLM_CPP_VULKAN=ON` appeared nowhere in
`ci.yml`** — the backend was built on no machine, so its whole suite ran nowhere.
`VK-A1` added the `build-test-vulkan` leg (llvmpipe, GPU-free) so this class of
rot is visible.

---

## 2. Scope — the target is the CPU backend's surface, and it is exactly 75 ops

User decision 2026-08-06: **"all 75 ops / every CPU-runnable model."** Resolved
against the tree, those two halves are the same number, which is why this scope
is well-posed:

> **CPU-registered (83) − Vulkan-registered (8) = 75 ops.**

So "full" means: **every op the CPU backend registers, Vulkan registers too** —
i.e. every model that runs on the portable CPU backend runs on Vulkan. This is
the right bar rather than 106, because **21 `OpId`s are vendor-specific by
construction** and are not owed on Vulkan at all: `kMatmulNvfp4Cutlass`,
`kMatmulFp8CublasLt`, `kMoeGroupedGemmNvfp4Marlin`, `kMarlinDenseGemm`,
`kSwizzleBlockscale`, `kLaguna`, the four `kDeepseekV4*`, the four NCCL
`kAllReduce`/`kAllGather`/`kSend`/`kRecv`, and the rest of the CUDA-only set.
Those are CUDA rows, not Vulkan gaps.

**The 75, grouped by sub-project.** Enumerated from the tree, not estimated:

| Group | Ops | Count |
|---|---|---|
| **B — dense bf16 e2e** | `kMatmul` `kMatmulBT` `kBatchedMatmul` `kPagedAttention` `kReshapeAndCache` `kEmbedding` `kQkvSplit` `kRopeNeox` `kRopeCosSinCache` `kRopeFromCache` `kAttnQkNormRopeGate` `kFusedNormRope` `kGeluTanh` `kGeluErf` `kGeluAndMul` `kMulScalar` `kMulColVecF32` `kSoftCap` `kL2Norm` `kRmsNormGated` `kIndexSelect` `kIndexCopy` `kGreedyArgmax` | 23 |
| **D — quant** | `kMatmulBTQuant` `kMatmulBTQuantGrouped` `kReshapeAndCacheFp8` `kRmsNormQuantFp8` `kRmsNormGatedQuantFp8` `kScaledFp4Quant` `kSiluMulFp4Quant` `kSiluAndMulFp4Quant` `kSigmoidGateFp4Quant` `kMatmulNvfp4Fp4` | 10 |
| **F — MoE** | `kMoeRouterTopK` `kMoeCombine` `kMoeSiluMul` `kMoeGateUpSwiGLUGrouped` `kSharedExpertGate` `kSigmoidGateBf16` `kAttnGateSplit` | 7 |
| **G — linear attention, MLA, conv** | `kGdnPrefill` `kGdnDecode` `kGdnSpecDecode` `kGdnPackedDecode` `kGdnGBeta` `kGdnConvSplit` `kGdnPostConv` `kGdnStateGather` `kGdnStateScatter` `kCausalConv1dFwd` `kCausalConv1dUpdate` `kCausalConv1dSpecUpdate` `kMlaDecodeAttention` `kMlaPrefillAttention` `kConcatAndCacheMla` `kConcatMlaNopeRope` `kGatherMlaCache` `kMergeAttnStates` `kMiniMaxH3` | 19 |
| **H — attention variants + samplers** | `kAttention` `kAttentionDenseFast` `kAttentionDenseFlash` `kDFlashBlockAttention` `kDFlashPagedBlockAttention` `kApplyTemperature` `kApplyTopKTopP` `kApplyMinP` `kApplyPenalties` `kApplyLogitBias` `kApplyTokenMask` `kApplyAllowedTokenIds` `kComputeProbs` `kComputeLogprobs` `kRandomSample` `kGreedyRejectionSample` | 16 |
| | | **75** |

**Explicitly out of scope for this spec:** ROCm, XPU, ANE, TPU, multi-GPU
(`kAllReduce`/`kSend`/…), and the CUDA-only vendor ops above.

**Note on the fp4 group.** `kScaledFp4Quant`/`kSiluMulFp4Quant`/
`kSigmoidGateFp4Quant`/`kSiluAndMulFp4Quant`/`kMatmulNvfp4Fp4` are in the CPU
surface because CPU carries reference implementations. On Vulkan they are only
worth kernels if NVFP4 weights are a Vulkan story; llama.cpp ships
`dequant_nvfp4.comp` and `dequant_mxfp4.comp`, so a source exists. `VK-D` opens
with a **scope call** on this, recorded either way.

---

## 3. Upstream chain and port sources

**Seam source (unchanged):** vLLM `vllm/platforms/interface.py:134-229`,
`:409-439`, `:181-187`; `vllm/v1/attention/backends/registry.py`;
`vllm/platforms/cpu.py:42-125` as the reference minimal platform.

**Kernel provenance rule for this campaign** — the hybrid the 7 landed shaders
already established, and the one every new shader follows:
> **Dispatch shape, pipeline structure and memory layout ported from llama.cpp
> with `file:line` citations; per-element math transcribed from OUR CPU kernel
> for the same `OpId`.** The first keeps us on a mature design; the second keeps
> the numerics inside the same gate band as CPU/CUDA, which is what makes the
> cross-device harness in `test_backend_cross_device` meaningful.

**Port-source map, verified present at pin `237ad9b96`:**

| Group | llama.cpp source | Note |
|---|---|---|
| GEMM scalar + coopmat1 | `mul_mm.comp` (466) | one file, scalar and coopmat1 selected by defines |
| GEMM coopmat2 | `mul_mm_cm2.comp` (658) | `VK_NV_cooperative_matrix2` |
| GEMM quant x quant | `mul_mmq.comp` (311), `mul_mmq_funcs.glsl` | the same-file GGUF path |
| GEMV / decode matvec | `mul_mat_vec.comp`, `mul_mat_vec_q{2,3,4,5,6}_k.comp`, `mul_mat_vecq.comp`, `mul_mat_vec_nc.comp`, `mul_mat_vec_p021.comp` | batch-1 decode is a GEMV problem, not a GEMM one |
| Attention | `flash_attn.comp` (758), `flash_attn_cm1.comp`, `flash_attn_cm2.comp` (481), `flash_attn_split_k_reduce.comp`, `flash_attn_mask_opt.comp`, `flash_attn_base.glsl` | **see the paging gap below** |
| Quant dequant | `dequant_q{2..6}_k.comp`, `dequant_q{4,5}_{0,1}.comp`, `dequant_q8_0.comp`, `dequant_iq*.comp`, `dequant_nvfp4.comp`, `dequant_mxfp4.comp`, `dequant_funcs.glsl`, `dequant_funcs_cm2.glsl` | ~25 files |
| MoE | `topk_moe.comp` (213), `count_experts.comp`, `add_id.comp`, `mul_mm_id_funcs.glsl` | routing + indexed GEMM |
| **GDN / linear attention** | **`gated_delta_net.comp` (189)**, `wkv6.comp`, `wkv7.comp`, `ssm_conv.comp`, `ssm_scan.comp`, `solve_tri.comp`, `cumsum*.comp`, `tri.comp` | see correction below |
| RoPE | `rope_neox.comp`, `rope_norm.comp`, `rope_multi.comp`, `rope_vision.comp`, `rope_funcs.glsl` | |
| Norms / activations | `rms_norm.comp`, `rms_norm_partials.comp`, `norm.comp`, `group_norm.comp`, `l2_norm.comp`, `swiglu.comp`, `geglu*.comp`, `unary.comp` | |
| Samplers | `argmax.comp`, `argsort.comp`, `argsort_large.comp`, `topk_argsort.comp`, `topk_nary_search.comp`, `soft_max.comp`, `soft_max_large{1,2,3}.comp` | |
| Copy / cache | `copy.comp`, `contig_copy.comp`, `copy_to_quant.comp`, `copy_from_quant.comp`, `get_rows*.comp` | |

**CORRECTION worth stating loudly, because it de-risks the hardest sub-project.**
An earlier reading of this campaign assumed llama.cpp's Vulkan backend has no
linear-attention reference and that `VK-G` would be authored from scratch against
our CUDA kernels. **That is wrong.** `gated_delta_net.comp` exists at our pin, is
189 lines, and its spec-constant block includes `KDA` — i.e. it covers **both**
the Qwen3-Next-class gated delta net **and** Kimi Delta Attention, the two
linear-attention families we carry. `solve_tri.comp`, `cumsum*.comp` and
`tri.comp` supply the chunked-delta-rule primitives alongside it. `VK-G` is a
port, not an invention. It stays last for size, not for risk.

**The one genuine port gap: paged KV.** `grep` over all 132 `.comp` + 26 `.glsl`
at the pin finds **zero** occurrences of `block_table`/`paged` — llama.cpp's FA
shaders assume a contiguous KV buffer plus a mask. Our `kPagedAttention` needs
block-table indirection. So `VK-B`/`VK-H` take **tile structure and the online-
softmax skeleton from `flash_attn.comp`** and **the block-table indirection from
our own CUDA paged kernel**, recorded as a partial-from-scratch entry in
`porting-inventory.md §9`. No other group has this problem.

---

## 4. Hardware and arena

| Device | Reachable | Role |
|---|---|---|
| **GB10 on `dgx.casa`** | YES — `NVIDIA GB10`, `INTEGRATED_GPU`, Vulkan API 1.4.312, vendor `0x10de`, 249 device extensions incl. **`VK_KHR_cooperative_matrix` v2** and **`VK_NV_cooperative_matrix2`**, `VK_KHR_shader_float16_int8`, `VK_KHR_{8,16}bit_storage`, `VK_KHR_shader_integer_dot_product`, `VK_KHR_timeline_semaphore`, `VK_EXT_memory_budget`, `VK_KHR_buffer_device_address`. One 89.72 GiB `DEVICE_LOCAL` heap with a `DEVICE_LOCAL|HOST_VISIBLE` type — unified | **PRIMARY. Correctness oracle box AND the optimization target** (user decision 2026-08-06). Both llama.cpp coopmat tiers are reachable |
| **`llvmpipe` (dev box)** | YES — Vulkan 1.4.318, CPU, `mesa-vulkan-drivers` | GPU-free CI correctness. **Never a speed venue** |
| **AMD RDNA / Intel Arc** | **NO — none on any box** | `VK-I`. Deferred by user decision; the only venue where a Vulkan win means something to a user, and the only thing that exercises the missing staging path |

**Premise update — the 2026-07-22 toolchain constraint is STALE.** That spec
determined the committed-SPIR-V route partly because *"neither box grants sudo"*
(`sudo -n true` → password required, measured 2026-07-22). As of 2026-08-06
`sudo -n true` **succeeds on the dev box**, and dgx has carried NOPASSWD sudo
since 2026-07-30. A coopmat2-capable `glslang`/`glslc`/Vulkan SDK is therefore
installable on both. This does **not** automatically reverse the decision — build
hermeticity is still worth keeping — but it removes the constraint that forced
it, which is exactly the question `VK-A1` has to answer. **`VK-E` needs this
anyway: llama.cpp's Vulkan backend requires `glslc` at build time**, so the
competitor cannot be built on dgx without it.

---

## 5. Gates

### 5.1 Correctness — three oracles, strongest first

| Stage | Oracle | Why it is available |
|---|---|---|
| Per-op | **CPU reference AND our CUDA backend, in the SAME binary on GB10** (`test_backend_cross_device`) | Already wired and already green for the 8 landed ops. This is the strongest op gate in the project and it costs nothing to extend |
| Quant | **f64 dequant-then-dot oracle**, `tests/vt/test_ops_quant_dot.cpp` (78,052 assertions, device-neutral) + `tests/vt/test_gguf_keep_quant.cpp` (5,574) | Both already pass on a second architecture; they were written device-neutral for exactly this |
| Model e2e | **our own CUDA backend on GB10, token-exact**, then the pinned vLLM oracle where the model has committed goldens | vLLM has no Vulkan path, so vLLM can never be a *device* oracle here — but it IS still the token oracle for the model, run on CUDA |

Bars, unchanged from the landed skeleton: **bit-exact** (`memcmp`) for `Copy`,
`Memset` and the bf16<->f32 codec; **NMSE ≤ 5e-4** for everything arithmetic. No
bit-exactness is claimed for reducing ops.

**The known non-reproducibility, carried forward, not rediscovered.** GLSL/SPIR-V
permit `a*b+c` contraction into FMA unless `NoContraction` is applied, and we do
not apply it: identical committed SPIR-V gives `kRmsNorm`@128 = `2.37e-14` on
llvmpipe and `2.92e-14` on GB10. Left as-is deliberately (ten orders inside the
bar; our CUDA kernels contract too; decorating every FMA would cost real speed on
a backend whose point is to be fast). If a row ever needs cross-implementation
bit-reproducibility, `NoContraction` is the lever.

### 5.2 Speed — the competitive gate

`BACKEND-GATE-VULKAN-LLAMACPP` / `BENCH-VK-LLAMA` activate at `VK-E`:

- **Same device** (GB10), **same GGUF file**, **same workload**, under
  [verification.md](../verification.md), holding `${GPU_LOCK}`.
- Competitor is **llama.cpp built with its Vulkan backend**, pinned at
  `237ad9b96` — the same commit we port from. `llama-bench` + server.
- **Three columns are recorded every time, not one:** ours-Vulkan,
  llama.cpp-Vulkan, and **ours-CUDA on the same box**. The third is free (same
  binary, same box, already wired) and it is the honest ceiling context — see §0.
- Per [benchmark gate statistics](../verification.md): calibrate bands from
  noise, discard cold first legs, reproduce 2-3x on an idle box. **Wall-clock e2e
  on GB10 swings with reload** — use steady-state or `ncu`-anchored measurement.

**Definition of done for the campaign:** 83/83 ops registered on Vulkan; the gate
models' architectures run token-exact against the CUDA-side oracle; and
ours-Vulkan ≥ llama.cpp-Vulkan on every throughput axis and ≤ on latency, on
GB10, reproduced. `VK-I` extends the same gate to an AMD board when one exists.

---

## 6. Sub-project decomposition

Ordered by two rules: **decisions that get more expensive the longer they wait go
first**, and **the denominator exists before anything is optimized.** Each is its
own spec → plan → implementation cycle with its own row and claim; this file is
the umbrella, not a substitute for them.

| ID | Sub-project | Blocked by | Deliverable |
|---|---|---|---|
| **VK-A1** | **Shader-variant pipeline + record repair** | — | **LANDED 2026-08-06 — see §6.1** |
| **VK-B** | **Dense bf16 model end to end** (23 ops) | A1 | A small dense model token-exact vs our CUDA backend on GB10, mirroring Metal's `M3a` (OPT-125m, 6/6 prompts / 96/96 tokens) |
| **VK-E** | **Competitive harness + `BENCH-VK-LLAMA`** | B | A pinned llama.cpp-Vulkan build on dgx and the first three-column number. **Activates `BACKEND-GATE-VULKAN-LLAMACPP`** |
| **VK-A2** | **Async dispatch + command-buffer graph capture** | B, E | `SupportsGraphCapture()` TRUE via pre-recorded `VkCommandBuffer`; submission off the per-op fence-wait. Measured against the E denominator |
| **VK-C** | **GEMM tactics: coopmat + coopmat2** | A2 | Measured tactic selection on GB10 through the arch-tactic registry, scalar fallback proven by `launch()→false` |
| **VK-D** | **Quant tier** (10 ops + the GGUF dequant surface) | C | NMSE ≤ 5e-4 vs the f64 oracle per encoding; same-file llama.cpp comparison becomes apples-to-apples |
| **VK-F** | **MoE** (7 ops) | D | A MoE model token-exact; grouped/indexed GEMM on the C tactics |
| **VK-G** | **Linear attention, MLA, conv** (19 ops) | F | GDN/KDA + MLA models running; ported from `gated_delta_net.comp` et al |
| **VK-H** | **Attention variants + samplers** (16 ops) | B (samplers), G (attn variants) | **83/83 — closes the op surface** |
| **VK-I** | **AMD/RDNA (or Arc) bring-up** | hardware acquisition | The staging path for non-host-visible memory, and the gate re-run where Vulkan actually matters |

### 6.0 `VK-G` partial: the two GATED-DELTA RECURRENCES landed — 2026-08-08

`row/BACKEND-VULKAN-GDN-CORE`. `kGdnPrefill` and `kGdnDecode` are native; the
Vulkan module count goes 22 -> 24 and the GDN family 6 -> 8. `kCausalConv1dFwd`,
`kRopeCosSinCache` and `kAttnQkNormRopeGate` stay on the reference tier.

**What was ported, and from where.** Per-step arithmetic 1:1 from
`src/vt/cpu/cpu_ops.cpp:1280-1311` `GdnHeadTokenStep`; dispatch shape and state
handling from our own `src/vt/cuda/cuda_gdn.cu:2417-2503` `GdnDecodeFusedKernel`
— one workgroup per (sequence, value-head, BV-value-tile), the `[BV,Dk]` state
slice staged into shared memory by a coalesced load and written back once, `NW`
lanes per value row splitting the `Dk` contraction. `BV=16`, `NW=8` are what
Vulkan's GUARANTEED 16 KB of shared memory allows at `VT_TG=128`; `BV=32` would
need 18 KB. Both shaders share one step body, `vt_gdn_recurrence.glsl`.

**THE ONE STRUCTURAL ADDITION over the CUDA kernel, and it is the prefill lever.**
CUDA's fused kernel is a DECODE kernel, so its coalesced load/store pair brackets
a single step. Prefill runs the SAME staged tile through the whole token range,
so a 512-token prompt touches the `[Dv,Dk]` state twice instead of ~2048 times.
That is legal because the VALUE ROW is an independent axis — row `vi` of `S` only
ever depends on itself, `k`, `q`, `decay`, `beta` and `v[vi]` — which CUDA already
exploits as its `grid.x` value tiling; only the sequence position is sequential,
and it stays sequential inside the workgroup.

**Measured (llvmpipe, correctness only — no speed claim is made or owed here).**
NMSE vs the CPU oracle in the same binary: prefill out `1.47e-14`, prefill
carried state `6.43e-15`, bf16 arm `0`; decode (indexed cache) out `1.64e-14` and
cache `3.31e-15`, decode (compact state) out `1.75e-14`. `test_vulkan_backend`
25/25 cases, 1020/1020 assertions. `test_opt_paged_engine` with
`VLLM_CPP_DEVICE=vulkan` still 6/6 token-exact (96/96), 0 declines.

**NOT measured: any speed number.** Local Vulkan is llvmpipe. The 27B
prefill/decode re-run on GB10, and the reference-tier count that goes with it,
are OWED and are the only thing that can turn the structure above into a result.

**A LATENT SEAM DEFECT THIS ROW HAD TO FIX.** `GetOpFallback` threw for any
native kernel that declined per-call, because the portable reference tier
installs only on a GetOp MISS and an op with a native kernel never misses. The
pre-existing `vt_paged_attn` fp8-KV decline had the same hole and had simply
never been exercised. `src/vt/op_provider.cpp` now installs the tier on that path
and drains the backend before handing a HOST kernel device memory.

**NOT taken, deliberately.** Rewriting the two passes as
`o = decay*(S·q) + v'*(k·q)` would read the state once instead of twice, but it
is an algebraic reassociation and would move the numbers off the CPU reference's
rounding for a saving that cannot be measured on llvmpipe. Recorded, not shipped.

### 6.1 `VK-A1` landed — 2026-08-06 (`CLAIM-VULKAN-FULL-1`)

**DECISION: keep the committed-SPIR-V route; make SPECIALIZATION CONSTANTS the
variant mechanism instead of GLSL `#define`s.** The build still needs no shader
toolchain on any machine. llama.cpp spells a variant as a `#define`, so every
dtype x quant x coopmat-tier combination is a whole new module — 242
`string_to_spv(` call sites at pin `237ad9b96`, most inside those loops. A
specialization constant is ONE module specialized at pipeline creation, with the
driver eliminating the branches the value kills, so artifact count tracks shader
FILES rather than their cross product.

| Delivered | Evidence |
|---|---|
| glslang **16.5.0** pinned (`$HOME/tools`, release tarball, nothing linked) | The recorded `16.4.0` **ships no release assets**, so it could never back a CI gate. Committed SPIR-V reproduces **byte-for-byte** under 16.5.0 — proving both that the artifact is what it claims and that the output survives a glslang minor bump |
| CI job `vulkan-spirv-freshness` | Pins the download URL (not a version string — the bytes are stable across versions). **Mutation-proved**: an equivalent rewrite of relu's select turns it RED, reverting turns it green |
| CI job `build-test-vulkan` | GPU-free on llvmpipe; runs the backend gate + cross-device numerics. Closes the hole that let the suite rot |
| SpecId metadata in the module table | Parsed from `OpDecorate SpecId` in the emitted module — glslang exposes no side channel, and a hand-kept list is the duplicate that drifts. `GetPipeline` checks the count, because Vulkan SILENTLY IGNORES an undeclared constantID (wrong numbers, not an error) |
| Specialized pipelines, cache keyed by specialization | `VkSpecializationInfo` built from named locals that outlive `vkCreateComputePipelines` — the escaping-temporary class this project hit twice under CUDA-graph capture |
| `vt_cast` dtype pair = the FIRST variant axis | Was a per-element push-constant branch; now constants 0 and 1. ONE module still serves every (src, dst) pair and the module got **SMALLER**, 109,436 → 109,216 bytes. Gated on the pipeline cache growing by TWO across two pairs — results alone prove nothing, since the defaults are f32→f32 |
| SPIR-V words moved to `vulkan_spirv.cpp` | Header 3,487 → **55 lines**. Adding shaders costs one TU's compile time, not every includer's. A test fails if the arrays reappear in the header |
| Descriptor-pool bug fixed | Pool was sized one set per MODULE; since specialization a module has many pipelines, each allocating a set, so it would have exhausted on the Nth specialization inside `vkAllocateDescriptorSets` |
| Record repaired | `feature-matrix.md:280` `INVENTORIED` → `ACTIVE`; §1.1 supersedes the "GetOp throws / 8 of 83" claims |

**MEASURED NEGATIVE RESULT — do not retry the workgroup size.** VT_TG is written
down three times (`vt_common.glsl`, each `.comp`'s `local_size_x`, and
`kWorkgroupSize` on the host, which derives the workgroup COUNT from it), exactly
the shape a specialization constant should collapse. It cannot at this target:
`layout(local_size_x_id = 0)` makes glslang emit `ExecutionMode LocalSize 1 1 1`
plus the legacy `BuiltIn WorkgroupSize` vector, because the modern `LocalSizeId`
mode needs SPIR-V 1.2 + `VK_KHR_maintenance4` (core in Vulkan 1.3) and this
backend targets `vulkan1.1` deliberately. On llvmpipe the literal `LocalSize 1`
wins: every workgroup runs ONE thread against a `ceil(n/128)` dispatch, and
cross-device NMSE went from ~1e-14 to **0.99 on `kAdd`**. An A/B isolated it —
keeping the constant and reverting only `local_size_x_id` is fully green. Raising
the target env would raise the device floor and is out of scope. The reasoning is
recorded beside the `#define` in `vt_common.glsl`.

**Deliberately NOT done:** the `vt::arch_tactics` generalization (§6 question 3).
A tactic registry with exactly one tactic is speculative; it belongs with `VK-C`,
which has a real second tactic to select.

**Gates:** clean `-Werror` build, Vulkan ON — 0 warnings. `test_vulkan_backend`
10/10 (405 assertions), `test_backend_cross_device` 6/6 (73, including the
bit-exact bf16 codec tier), generator suite 9/9, `--check` green on both generated
files. `CMakeLists.txt`'s tri-state is untouched, so `AUTO` still resolves OFF and
the CUDA gate build is unaffected by construction. **No speed number measured,
claimed or owed.**

### Why `VK-A1` is first, and why it is not bikeshedding

The committed-SPIR-V header is **3,491 lines for 7 modules**. The target surface
is ~100 shaders, and shaders are not the unit — **pipelines** are: llama.cpp's
generator has **242 `string_to_spv(` call sites**, most inside loops over dtype x
quant-type x coopmat-tier, and **35 `coopmat` references** driving tier variants.
The real artifact count is in the high hundreds. A single committed C-array header
does not survive that, and every shader written before the decision is a shader
written twice.

`VK-A1` must answer, with the sudo constraint now lifted (§4):
1. **Build-time generation** (llama.cpp's model — needs `glslc` on every build
   box, which we can now install, but breaks CI hermeticity and the
   `llvmpipe`-only path) **vs. committed artifacts** (hermetic, but needs
   splitting/compression and a `--check` staleness gate that actually runs in CI)
   **vs. a hybrid** (commit a core set, generate variants).
2. How coopmat tiers are spelled — GLSL defines like llama.cpp, or SPIR-V
   specialization constants (which `gated_delta_net.comp` itself uses for
   `S_V`/`KDA`/`SUBGROUP_SIZE`, and which collapse variant count).
3. Where the tier is *selected* — this is the `vt::arch_tactics` generalization
   deferred as `W0b` item 8, and Vulkan is now its second consumer.

### Why `VK-E` sits before every optimization row

`VK-A2` and `VK-C` are optimization rows. Optimizing without a denominator is the
failure this project has already paid for more than once. `VK-E` is deliberately
cheap — build llama.cpp with `-DGGML_VULKAN=ON` on dgx, run `llama-bench` on the
same GGUF — and it converts every later row from "faster than before" into
"fraction of the competitor".

### The `W0b` debt, now on the critical path

The predecessor spec deferred four shared-seam items as *"not yet needed: they
block a MODEL on a non-CUDA backend, and W0 ships no model."* `VK-B` ships a
model, so they land with it:

| # | Item | File |
|---|---|---|
| 5 | guard the 4 `vt/cuda/` includes + the public header | `include/vllm/model_executor/models/dense_nvfp4_gemm.h:66` |
| 6 | hardcoded `kCUDA` queue → `CurrentPlatform().device_type()` | `src/vllm/entrypoints/model_loader.cpp:39` |
| 7 | **SPLIT** `QuantTypeTraits` out of `vt::cpu` (not lift — two of its five fields are implementation facts, not encoding facts, per [metal-mlx-reuse-study.md](metal-mlx-reuse-study.md)) | `include/vt/quant.h` |
| 8 | generalize `cuda_arch_tactics.h` into backend-agnostic `vt::arch_tactics` | new `include/vt/arch_tactics.h` |

Metal hit items 5-7 already at its `M3a`; Vulkan should reuse whatever that
landed rather than re-deriving it. Item 8 is `VK-A1`'s (§6) and `VK-C`'s.

---

## 7. Tests

Per [porting.md](../porting.md). Vulkan has **no upstream vLLM tests to
port** — the inventory is explicit about reuse vs. new authorship.

| Test | Disposition |
|---|---|
| `tests/vt/test_backend_cross_device.cpp` | **Extend per sub-project.** The CPU-vs-CUDA-vs-Vulkan harness in one binary is the spine of every correctness gate here |
| `tests/vt/test_vulkan_backend.cpp` | **Extend.** It asserts the registered AND the stubbed set as executable facts; each sub-project moves ops across that line, and the assertion must move with them |
| `tests/vt/test_ops_quant_dot.cpp` | **Reuse verbatim** at `VK-D` — device-neutral f64 oracle |
| `tests/vllm/test_gguf_keep_quant.cpp` | **Reuse verbatim** at `VK-D` |
| `tests/vt/test_fused_chain_additivity.cpp` | **Reuse verbatim** — it deliberately uses a REAL second backend |
| `tests/vllm/platforms/test_platform.cpp` | Extend with Vulkan capability/priority cases as `get_attn_backend_priority()` stops being empty |
| NEW | A `--check` CI job for the SPIR-V artifact staleness gate (`VK-A1`) — today `gen-vulkan-spirv.py --check` exists but no CI job runs it |
| NEW | `VK-E` harness scripts under `benchmarks/`, following the existing competitive-benchmark shape |

**Every sub-project runs the full CPU ctest and the CUDA gate suite unchanged.**
`VLLM_CPP_VULKAN` AUTO resolves OFF, so the SACRED gates are untouched by
construction — that property is load-bearing and must be re-verified, not
assumed, on any change to CMake or the registrars.

---

## 8. Risks and decisions

1. **DECISION (user, 2026-08-06) — completeness before optimization.** The
   campaign reaches a running, correct backend first; "optimized" is measured
   only after `VK-E` exists. Rationale: nothing about optimization is even
   measurable at 8/83 ops.
2. **DECISION (user, 2026-08-06) — GB10 is the optimization target; AMD is a
   later acquisition.** Recorded consequence: the staging path stays untested and
   `VK-I` carries it.
3. **RISK — the win is softer than it sounds.** See §0. Mitigation is disclosure,
   not engineering: the three-column record in §5.2.
4. **RISK — shader-variant explosion** (§6). Mitigated by ordering `VK-A1` first.
   Residual: if the decision is "build-time generation", the GPU-free `llvmpipe`
   CI path acquires a `glslc` dependency it does not have today. `VK-A1` must
   state which CI legs change.
5. **RISK — per-op fence-wait is an architectural speed cap, not a constant
   factor.** Decode in this project is repeatedly measured host-orchestration-
   bound; a synchronous record+submit+wait per op will dominate everything `VK-C`
   can win in the GEMM. If `VK-E`'s first number is catastrophic rather than
   merely behind, `VK-A2` is the cause and should be pulled ahead of `VK-C`. This
   is an anticipated re-order, recorded now so it is not read later as a slip.
6. **RISK — paged-attention indirection has no Vulkan port source** (§3). The
   only group in the campaign where structure must be authored. Recorded in
   `porting-inventory.md §9` when it lands.
7. **RISK — `glslc` on dgx is a `VK-E` prerequisite**, since llama.cpp's Vulkan
   backend needs it at build time. Now unblocked by sudo (§4), but it is an
   install on the gate box and must be recorded in
   [environment.md](../environment.md) when done.
8. **OPEN — fp4 on Vulkan** (§2). `dequant_nvfp4.comp`/`dequant_mxfp4.comp` exist
   upstream, so it is possible; whether it is *wanted* is `VK-D`'s scope call.
9. **RISK — Vulkan has gone stale once already.** It landed in one commit and sat
   for two weeks while Metal advanced 8 → 19 ops and ran a perf campaign. A
   multi-sub-project campaign is more exposed to this, not less. Mitigation: each
   sub-project carries its own row and gate, so a pause is visible in the record
   rather than silent.

---

## 9. Record obligations

Nothing in this change moves a lifecycle state. On landing this spec:
- `docs/STATUS.md` and `docs/BENCHMARKS.md` record the plan-only outcome
  explicitly (required by `scripts/check-doc-checkpoint.py` for any
  `.agents/specs/` change).
- `docs/FEATURES.md` does **not** move — no feature surface changed.
- `BACKEND-VULKAN` stays `ACTIVE` (gated skeleton). `BACKEND-GATE-VULKAN-LLAMACPP`
  and `BENCH-VK-LLAMA` stay `INVENTORIED` until `VK-E` produces a number.
- The `feature-matrix.md:280` drift is repaired by `VK-A1`, not here, so that the
  repair rides with the change that re-counts the op surface.
