# Upstream sync report — 2026-09-01, target `adebc41`

Wave `SYNCENUM`. Steps 1 to 4 of the cycle in
[`../upstream-sync.md`](../upstream-sync.md). This report ports nothing and does
not move the pin. It is the reviewable artifact and the work queue for step 5.

Issue: [#2490](https://github.com/mudler/vllm.cpp/issues/2490).

| Field | Value |
|---|---|
| Active pin | `5559679229bc961848b121ccdeaa8fa5d79bec98`, 2026-07-26 |
| Target | `adebc41b7e9f1085d3f73434e23beb76883b9eb4`, 2026-09-01 16:26 +0200 |
| Target subject | `[Mamba] Add FlashInfer ReplaySSM backend (#52506)` |
| Reference checkout | `${VLLM_SOURCE}`, `origin` = `https://github.com/vllm-project/vllm` |
| Fetched | 2026-09-01, `git fetch origin main`, exit 0 |
| Pin moved | NO. The cycle stops after step 4. |

The developer named `25efcfa788` as the target. `git fetch origin main` advanced
`origin/main` to `adebc41b7e` during this wave, so the report targets
`adebc41b7e` and names it everywhere. `25efcfa788` is its parent's ancestor and
is one commit behind.

## 1. The finding that bounds this whole report

**The reference checkout is a SHALLOW repository, so the pin is not an ancestor
of the target and `git log PIN..TARGET` is not a delta.**

```console
$ git -C ${VLLM_SOURCE} rev-parse --is-shallow-repository
true
$ cat ${VLLM_SOURCE}/.git/shallow
16282a9c4ee754bedd67f72b01ac76ae8568dbdd
41f179b57aa8ab6f634f508128ce1f1efadd0eb1
d79a675f332aa82cb4c849d46ce37dbf496e55fb
$ git -C ${VLLM_SOURCE} merge-base --is-ancestor 5559679229 adebc41b7e; echo $?
1
$ git -C ${VLLM_SOURCE} merge-base 5559679229 adebc41b7e; echo $?
1
$ git -C ${VLLM_SOURCE} rev-list --max-parents=0 adebc41b7e
41f179b57aa8ab6f634f508128ce1f1efadd0eb1
$ git -C ${VLLM_SOURCE} rev-list --count adebc41b7e
595
$ git -C ${VLLM_SOURCE} rev-list --count 5559679229
1653
```

`.git/shallow` grafts `41f179b57a` (2026-08-18,
`[Rust Frontend] Simplify data-parallel size ownership (#52575)`) as an
artificial root. That commit reports 6619 files and 1939079 insertions with no
parent, because a shallow boundary presents its whole tree as one addition.

Three consequences bind every number in this report.

1. `git log 5559679229..adebc41b7e` returns the **entire 595-commit truncated
   history**, not the change since the pin. The count reads like a delta and is
   not one. The "594 commits ahead" figure the wave started from is the same
   artifact, one commit older.
2. Every upstream commit between 2026-07-26 and 2026-08-18 is **absent from the
   checkout as a commit**. Twenty-three days of upstream history cannot be
   enumerated, classified, or cited from here.
3. A tree-level diff across that invisible window still measures the file
   change. **212 files we have already ported changed, +19509 and -6591 lines**,
   with no commit to attribute them to:

   ```console
   $ git -C ${VLLM_SOURCE} diff --numstat 5559679229 41f179b57a -- <369 ported paths> | wc -l
   212
   ```

   The largest are `muse_glimmer.py` (1649 lines changed),
   `layers/attention/mla_attention.py` (1219), `dots3_note/nvidia/attention.py`
   (807), `tool_parsers/mistral_tool_parser.py` (788),
   `v1/worker/gpu/model_runner.py` (629),
   `layers/attention/sparse_mla_attention.py` (625),
   `v1/worker/mamba_utils.py` (595) and `v1/worker/gpu_model_runner.py` (592).

**Step 1 of the next cycle must run `git fetch --unshallow origin` in the
reference checkout before the enumeration can be complete.** That is a large
download and needs developer authority under AGENTS.md §Commands. Until it runs,
no sync report can claim a complete commit-level classification, and this one
does not.

The checkout also carries a stale `refs/heads/main` at `e24d1b24fe`, the
pre-`W5` starting pin, and local branches `pr-51655-muse` and `pr52197`. Those
branches, not `origin/main`, are what still contain the pin object.

## 2. Result

| Disposition | Commits |
|---|---:|
| PORT-NOW | 122 |
| INVENTORY | 125 |
| IGNORE | 182 |
| UNCLASSIFIED | 1 |
| **Classified total** | **430** |

The 430 commits are the ones that touch the mirrored surface, out of the 595 the
truncated range prints. The other 165 touch no mirrored path: continuous
integration, documentation, `csrc/`, `tests/`, packaging and the Rust frontend
crate.

Two mechanical checks ran over the finished lists.

```console
$ comm -23 classified.sha expected.sha   # classified but never enumerated
$ comm -13 classified.sha expected.sha   # enumerated but never classified
$ uniq -d classified.sha                  # duplicates
```

All three are empty: the classification is a bijection onto the enumeration.
A second check reads each cited `vllm#NNNNN` back from `git log -1 --format=%s`
and requires the subject to contain `(#NNNNN)`. It reports 0 mismatches over all
430 lines, so no PR number in this report is invented.

### Where `vllm#53896` landed: **PORT-NOW**, and it is also the largest INVENTORY item

`e126687a9a` `[Model] Support Qwen3.8-Flash-Next (#53896)`, 2026-08-31, 124 files,
19425 insertions and 451 deletions. It is filed PORT-NOW rather than INVENTORY,
and the reason is not the new architecture.

It changes **24 files this tree has already ported**:

```text
vllm/v1/core/kv_cache_utils.py                   +193/-156
vllm/v1/worker/mamba_utils.py                    +143/-58
vllm/v1/core/single_type_kv_cache_manager.py      +97/-2
vllm/v1/kv_cache_interface.py                     +59/-4
vllm/model_executor/layers/quantization/fp8.py    +53/-13
vllm/config/speculative.py                        +43/-0
vllm/model_executor/models/qwen3_next.py          +62/-3
vllm/v1/core/kv_cache_coordinator.py              +14/-5
vllm/v1/worker/gpu/model_runner.py                +17/-2
vllm/v1/worker/gpu_model_runner.py                +15/-3
vllm/v1/worker/gpu/block_table.py                 +15/-1
vllm/model_executor/models/interfaces.py          +27/-1
vllm/platforms/interface.py                       +12/-5
vllm/v1/core/sched/scheduler.py                   +13/-0
vllm/model_executor/layers/fused_moe/routed_experts.py  +14/-0
vllm/model_executor/models/registry.py             +9/-0
vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py  +5/-4
vllm/v1/core/kv_cache_manager.py                   +8/-0
vllm/model_executor/layers/mamba/abstract.py       +5/-0
vllm/transformers_utils/model_arch_config_convertor.py  +10/-0
vllm/transformers_utils/config.py                  +2/-0
vllm/_custom_ops.py                                +2/-0
vllm/config/compilation.py                         +3/-0
vllm/config/scheduler.py                           +4/-0
```

`kv_cache_utils.py` is reworked, not extended: generic block-size resolution
becomes a prefix-cacheable-only GCD with per-state alignment checks, and
`_get_packed_kv_cache_groups` replaces `group_and_unify_kv_cache_specs`. That
changes KV grouping for models this tree already runs, independently of
Qwen3.8-Flash-Next. `kv_cache_interface.py`, `single_type_kv_cache_manager.py`
and `mamba_utils.py` follow it.

The architecture itself is 31 new files under `vllm/models/qwen4_exp/`, with
separate `nvidia/` and `amd/` backends (`ple_layer.py`, `qsa.py`, `ops/qsa.py`,
`ops/qsa_pre_indexer.py`, `indexer_qsa.py`, `hyperconnection.py`, `ops/hc.py`,
`mtp.py`, `low_latency_gemm.py`, `model_state.py`, `common/qsa_cache.py`,
`common/ple.py`, `config.py`), plus `vllm/transformers_utils/configs/qwen4_exp.py`
and a `tests/models/qwen4_exp/` and `tests/evals/qwen4_exp/` suite.

`vllm/model_executor/models/registry.py` at the target registers
`Qwen4ExpForCausalLM`, `Qwen4ExpForConditionalGeneration` and `Qwen4ExpMTP`. Each
key appears exactly once at the target and zero times at the pin:

```console
$ git show adebc41b7e:vllm/model_executor/models/registry.py | grep -c '"Qwen4ExpForCausalLM":'
1
$ git show 5559679229:vllm/model_executor/models/registry.py | grep -c '"Qwen4ExpForCausalLM":'
0
```

**Consequence for the record.** AGENTS.md makes vLLM the only reference wherever
it implements the behavior. The developer direction of 2026-08-26 made
`transformers` the algorithm oracle for `MODEL-MM-QWEN4-EXP` on the stated
premise that vLLM implemented nothing, and the lane exception in
[`../oracles/transformers.md`](../oracles/transformers.md) names its own expiry:
"it expires the moment vLLM registers `qwen4_exp`". That moment was 2026-08-31.
The reconciliation is owned by
[#2489](https://github.com/mudler/vllm.cpp/issues/2489), not by this report, and
this report is the evidence that the premise expired.

### The target commit is itself an INVENTORY item

`adebc41b7e` `[Mamba] Add FlashInfer ReplaySSM backend (#52506)` adds a new Mamba
decode backend and redefines `replayssm_buffer_len` as a logical length in
`config/cache.py` (Triton takes B rows, FlashInfer takes B+1).

## 3. Method: the mirrored subtrees, and how they were derived

The guide says the ported surface is derivable because our structure mirrors
upstream one to one. It was derived, not assumed:

```console
$ grep -rhoE 'vllm/[A-Za-z0-9_/]+\.py' src/ include/ tools/ | sort -u
385 paths
```

Of those 385 cited upstream paths, **369 exist at the pin, at the target, or at
both**. Those 369 are the ported-file set used in this report. The other 16 never
existed at either revision and are listed in §7.

The enumeration then ran in two tiers.

**Tier A, 259 commits** touch at least one of the 369 ported files:

```console
$ git log --oneline --no-merges 5559679229..adebc41b7e -- <369 ported paths> | wc -l
259
```

**Tier B, 171 commits** touch one of the 99 mirrored directories (the `dirname`
of each ported path, with the repository root excluded so the pathspec does not
degenerate to the whole tree) or one of the 10 mirrored root-level modules, but
touch no ported file. Tier A and Tier B are disjoint and cover 430 commits.

The 99 subtrees, ranked by commits in the window and by files changed across the
invisible window of §1:

| Subtree | Commits | Files changed pin to graft |
|---|---:|---:|
| `vllm/model_executor` | 171 | 427 |
| `vllm/v1` | 153 | 198 |
| `vllm/model_executor/models` | 88 | 195 |
| `vllm/model_executor/layers` | 87 | 172 |
| `vllm/v1/worker` | 60 | 63 |
| `vllm/config` | 50 | 17 |
| `vllm/v1/worker/gpu` | 47 | 47 |
| `vllm/v1/attention` | 47 | 56 |
| `vllm/entrypoints` | 45 | 78 |
| `vllm/v1/attention/backends` | 42 | 35 |
| `vllm/distributed` | 42 | 58 |
| `vllm/model_executor/layers/fused_moe` | 34 | 63 |
| `vllm/multimodal` | 26 | 20 |
| `vllm/model_executor/layers/quantization` | 25 | 71 |
| `vllm/v1/attention/backends/mla` | 22 | 18 |
| `vllm/v1/core` | 21 | 10 |
| `vllm/models/deepseek_v4` | 20 | 23 |
| `vllm/distributed/kv_transfer/kv_connector` | 20 | 28 |
| `vllm/v1/engine` | 19 | 10 |
| `vllm/entrypoints/openai` | 19 | 14 |

The remaining 79 subtrees each carry fewer than 19 commits. The 10 root-level
modules are `_custom_ops.py`, `envs.py`, `__init__.py`, `logger.py`,
`logprobs.py`, `outputs.py`, `pooling_params.py`, `sampling_params.py`,
`scalar_type.py` and `tasks.py`.

### The upstream layout moved, and `vllm/models/` is not new

`vllm/models/` already existed at the pin, holding `deepseek_v32`,
`deepseek_v4`, `inkling` and `minimax_m3`. At the target it also holds `common`,
`dots3_note`, `hy_v4`, `kimi_k3` and `qwen4_exp`. `vllm/model_executor/models/`
still exists and is still the larger directory, so the two are parallel homes
rather than a completed migration. **Both were enumerated.** A path-based sweep
that assumed only `vllm/model_executor/models/` would have missed `qwen4_exp`
entirely, which is the commit this cycle exists for.

One new top-level directory appeared in the window: `vllm/tilelang_utils`. It has
no add commit in the visible history, so it arrived in the invisible window.
This tree cites nothing under it.
