# PORT-NOW tranche 5 of `5559679229..e126687a9a`, re-derived against this tree

Wave PORTQ-5. Row `UPSTREAM-SYNC-HEADPIN`.
Issue: [#2679](https://github.com/mudler/vllm.cpp/issues/2679).
Spec: [`../specs/upstream-sync-portq5.md`](../specs/upstream-sync-portq5.md).
Tree read: `origin/main` at `a700e8da6`.
Upstream read at revision, never in a working tree.

**Nothing here was executed.** No build, no test run, no GPU, no lease. Every
label is a static reading of source. An `ALREADY_SATISFIED` below means the code
implements the behaviour, not that a gate observed it doing so.

## 1. The answer

Of **PORT-NOW entries 161 to 200** of `5559679229..e126687a9a` in upstream commit
order:

| | Count |
|---|---:|
| ALREADY_SATISFIED | **3** |
| REAL_GAP | **10** |
| NOT_APPLICABLE | **27** — 26 `surface-absent`, 1 `inert` |
| **Disagree with the recorded disposition** | **30 of 40** |

A `PORT-NOW` label asserts that portable work exists in this tree. Thirty of
these forty do not: either the tree already has the behaviour, or the surface the
commit edits is not here at all.

**The record's readings of the upstream diffs were accurate in all forty cases**,
as they were for tranches 1, 2 and 3. Not one was wrong about what the commit
does. What thirty of them missed is this tree.

Ten issues carry the ten gaps. Nine are new
([#2686](https://github.com/mudler/vllm.cpp/issues/2686),
[#2687](https://github.com/mudler/vllm.cpp/issues/2687),
[#2690](https://github.com/mudler/vllm.cpp/issues/2690),
[#2691](https://github.com/mudler/vllm.cpp/issues/2691),
[#2692](https://github.com/mudler/vllm.cpp/issues/2692),
[#2695](https://github.com/mudler/vllm.cpp/issues/2695),
[#2698](https://github.com/mudler/vllm.cpp/issues/2698),
[#2699](https://github.com/mudler/vllm.cpp/issues/2699),
[#2701](https://github.com/mudler/vllm.cpp/issues/2701)); the tenth, `[162]`, was
already filed by PORTQ-2 as
[#2657](https://github.com/mudler/vllm.cpp/issues/2657) and needed no second
issue.

**Nine of the forty rest on a pre-pin hole** — the surface upstream edits already
existed at `5559679229` and was never ported here, so the commit is not the
distance. **Three entries are structurally non-work**, which is a defect in the
queue rather than a judgement about the tree; §5 gives them.

## 2. Method, and the checks that license it

### 2.1 The tranche

```console
$ git rev-list 5559679229..e126687a9a | cut -c1-10 | sort -u | wc -l          # 1465
$ comm -12 <(sort portnow.txt) range.txt | wc -l                              # 290
$ git rev-list --reverse 5559679229..e126687a9a | cut -c1-10 \
    | grep -x -F -f portnow_range.txt | sed -n '161,200p'
```

`portnow.txt` is the 315 SHAs of [`2026-09-01-cdefd9d.md`](2026-09-01-cdefd9d.md)
§4. Both `1465` and `290` reproduce the counts
[`2026-09-02-e126687.md`](2026-09-02-e126687.md) §4 publishes, so the extraction
reproduces a committed number before anything is read off it.

**The ordering was checked twice, and against three landed tables rather than
one.** Positions 1, 9, 21, 40 come out `3f1d40960f`, `bf2b45b5d6`, `0b6aa3c47c`,
`b49eaf205a` — exactly what [`2026-09-03-portq1.md`](2026-09-03-portq1.md) §3
names. Positions 41, 60, 80 resolve in PORTQ-2's table and 81, 95, 116, 120 in
PORTQ-3's. The three landed tranches are contiguous, disjoint and correctly
ordered against this derivation, so the slice at 161-200 is the same extraction
they were.

Second check, in absolute time: `git rev-list --merges` over the range returns
**0**, and committer timestamps (`%ct`) are **strictly monotone across all 290**,
verified entry by entry. This is PORTQ-3's retracted-and-repaired check, and it
holds. The tranche spans rendered local dates 2026-08-13 to 2026-08-18, and that
field inverts without meaning — `[161]` renders `2026-08-13T21:51:24+00:00` and
`[162]` renders `2026-08-13T15:08:07-07:00`, which is `22:08:07Z` and later.
**Compare `%ct`, never `%cd`.**

Had either denominator, the positional check or the monotonicity failed, this
wave would have stopped, because that would have invalidated three landed
tranches as well as this one.

### 2.2 The labels

`ALREADY_SATISFIED` cites the `file:line` or symbol that satisfies the entry.
`REAL_GAP` says what is missing, roughly how big, and who owns it.
`NOT_APPLICABLE` splits, because the two halves have different futures:

- **`surface-absent`** — the commit edits something this tree does not carry.
  Discarded work; there is nothing to port until the underlying feature is.
- **`inert:<gate>`** — the surface is here and the new arm is gated on something
  unreachable in every configuration this tree builds. **Deferred** work: it
  becomes real the day that gate is ported, so the gate is named.

`inert` requires the surface to be here. Where half a commit has no surface at
all, that half is `surface-absent`: flipping the gate would land on nothing. Only
`[185]` earned `inert`, and §7 says why that call was close.

### 2.3 A fifth and a sixth false-zero mechanism, both found in this wave

The brief carried the four the earlier tranches paid for. Two more appeared here,
and the first one bit the operator, not a reader.

**Fifth: `grep` in this shell is a function wrapping `ugrep`, not GNU grep, and
without `-r` it skips a directory argument in complete silence.**

```console
$ grep -n  'GetFreedMmHashes' src include   # nothing, empty stderr, rc=1
$ grep -rn 'GetFreedMmHashes' src include   # 4 hits, rc=0
$ /usr/bin/grep -n 'GetFreedMmHashes' src include
/usr/bin/grep: src: Is a directory
/usr/bin/grep: include: Is a directory      # rc=2
```

This is worse than the four already recorded, because the usual defence fails.
GNU grep answers **rc 2** — "your probe was malformed" — and prints a diagnostic.
`ugrep` answers **rc 1**, which is the canonical "no match found". A
well-formed-looking probe returns the canonical absence answer leaving no trace.
It was caught here only because the same concept was greppable under a second
spelling, which is exactly what §2.4's two-spellings rule is for. All six readers
were sent the mechanism mid-wave and re-ran their zeros; every zero recorded
below carries `-r`.

**Sixth: `-F` does not do alternation either.** Two readers independently wrote
`grep -rniF "a\|b\|c"`, which under `-F` searches for one long literal string
containing backslashes and pipes, and returns zero. The brief's own advice to
"prefer `grep -rF` fixed strings" invites this, so it now carries a clause: **one
needle per call.** This is distinct from the malformed-ERE mechanism already
recorded, which bites under `-E`.

A seventh was also met and is a repeat of a known zsh trap rather than a new
mechanism: an unquoted `$scope` variable holding several paths does not
word-split in zsh, so the probe searched one nonexistent directory. Its reader
caught it because the **positive controls went to zero too** — which is the whole
argument for running them.

### 2.4 Absence is never concluded from one grep, and every zero carries a control

Each entry was searched under at least two spellings and in at least two
locations before anything was called missing, the searches are recorded beside
the claim, **every zero is paired with a positive control run through the same
probe form**, and every zero states its scope. §8 lists what each
`NOT_APPLICABLE` resting on absence rests on.

The rule earned its keep twice this wave. `[196]`'s first probe suggested this
tree has no `is_hybrid` at all; a `-r` probe found 45 hits, and the truth is
narrower and more interesting than either — see §6.2. `[186]`'s reader found
`max_loras` 37 times and had to open all 37 to establish that none of them is the
handshake field the commit adds.

### 2.5 Git and the pin were asked before any novelty was claimed

`git log --oneline --grep '<sha>'`, `--grep '<upstream PR number>'`,
`git log -S'<symbol>'` and a grep of `.agents/` for the SHA, then PORTQ-3's
cheaper check, `git show 5559679229:<upstream path>`.

**No commit in this repository's history triages any of these forty by SHA or by
PR number** — every `git log --grep` came back empty. Five entries had a prior
reading in `.agents/`, and §6 reconciles them. The pin check was the more
productive of the two: it settled or reframed **nine** entries (§4.2).

### 2.6 The reading was delegated, and the citations were re-read

Six fresh readers took six or seven entries each, clustered by surface. The
operator of this wave printed the citations again. **All ten real gaps and
nineteen further citations were re-read**, including every `file:line` behind a
`REAL_GAP`, every recorded prior triage, and every pre-pin claim that carried a
verdict.

**One citation was wrong and is corrected here.** `[191]`'s reader cited the
owning row as `.agents/engine-matrix.md:131`. `SAMPLE-LOGPROBS` is at **`:142`**;
`:131` is a different row. The reader took the anchor from
`.agents/specs/logprobs-all-sentinel.md`, whose header carries the same stale
`:131` — so the reader repeated a committed record's drift rather than inventing
one. Substance correct, anchor wrong. No citation named a symbol that does not
exist, and every other row anchor in §4 was re-resolved by line number.

## 3. The forty

Ordered oldest first. **"Holds?"** is whether the recorded `PORT-NOW` disposition
holds for this tree. `PORT-NOW` asserts that portable work exists here, so it
holds exactly where the label is `REAL_GAP` and nowhere else — the column and the
§1 count of 30 are the same number read two ways.

The column says nothing about whether the record read *upstream* correctly. It
did, in all forty.

| # | upstream | vllm# | label | holds? | evidence |
|---:|---|---|---|---|---|
| 161 | `63550515b4` | 45423 | NA surface-absent | no | This tree does not port upstream's Python benchmark harness. `benchmarks/` holds only C++ microbenchmarks; the benchmark client is `examples/bench/bench_core.h:73-88`, whose only two prompt sources are synthetic random and a ShareGPT JSON file (`:521-546`). No dataset registry, no `SampleRequest`. The substance is already the default anyway: `bench_core.h:227-233` submits token IDs, not a decoded string. |
| 162 | `51def7848a` | 52223 | **REAL_GAP** | yes | The k-floor refusal is here and reached: `include/vllm/config/speculative.h:364-376`, sole production caller `src/vllm/entrypoints/model_loader.cpp:1630`, pinned by two suites (387 + 216 lines). Upstream deleted it. Already filed by PORTQ-2 as [#2657](https://github.com/mudler/vllm.cpp/issues/2657). |
| 163 | `38f097fab8` | 51796 | **REAL_GAP** | yes (half) | `StructuredOutputsParams::Verify()` (`src/vllm/sampling_params.cpp:196-213`) never inspects `regex`'s contents. The `validate_xgrammar_grammar` half is surface-absent and pre-pin. [#2701](https://github.com/mudler/vllm.cpp/issues/2701). |
| 164 | `b216db3ed0` | 51795 | **REAL_GAP** | yes (larger) | Neither bound exists. `src/vllm/v1/engine/input_processor.cpp:286-317` validates only params and prompt length. The **upper** bound was already at the pin, so the gap is the whole check. [#2699](https://github.com/mudler/vllm.cpp/issues/2699). |
| 165 | `c05d75aaa9` | 50685 | NA surface-absent | no | No sequence-parallel MoE at all: `use_attn_reduce_scatter_for_moe`, `reduce_scatter`, `ReduceScatter`, `use_sequence_parallel_moe` → 0 in `src/`+`include/`+`tests/`. No `qwen3_next.cpp` exists. Owed by the capability row at `.agents/feature-matrix.md:144`, which names inventory row `PAR-SEQUENCE-MOE` (also `.agents/porting-inventory.md:68`); note `feature-matrix.md` rows are keyed by capability name, not by row ID. |
| 166 | `63a9a5010a` | 52164 | NA surface-absent | no | This tree computes DSA selection with its own `vt::DsaIndexerLogits` / `vt::DsaTopkSelect` (`include/vt/ops.h:1989-1996`, `:2538-2541`), never DeepGEMM's paged-MQA-logits kernel. No scheduling-metadata buffer to size. |
| 167 | `57bd0ed441` | 51704 | **REAL_GAP** | yes (split) | `head_size_v` is still per-subclass (`include/vllm/v1/kv_cache_interface.h:239`, `:352`); five `real_page_size_bytes` overrides carry the page-size math (`src/vllm/v1/kv_cache_interface.cpp:83-193`). The turboquant half is surface-absent. **Must not land alone** — see §5.3. [#2695](https://github.com/mudler/vllm.cpp/issues/2695). |
| 168 | `cdc4824a21` | 48684 | NA surface-absent | no | A pure removal of a field this tree never had. `override_attention_dtype`, `override-attention-dtype`, `OverrideAttentionDtype`, and even the loosest substring `attention_dtype` → 0 across `src include tests tools benchmarks examples scripts docs`. |
| 169 | `97388c44f9` | 51538 | NA surface-absent | no | `src/vllm/v1/attention/backends/` holds only `cpu_attn.cpp`, `gdn_attn.cpp`, `utils.cpp`. `sparse_swa.py` appears here only as a citation for KV geometry (`deepseek_v4_registry.cpp:305-306`). Pre-pin: the metadata builder and the DSpark non-causal width were both at the pin. |
| 170 | `6593754e61` | 52419 | NA surface-absent | no | `enable_partial_hash_hits` → 0 in `src/`+`include/`+`tests/`, so the predicate that selects the new arm does not exist; `_align_cacheable` would be an identity rewrite of `src/vllm/v1/core/kv_cache_coordinator.cpp:447-463`, which already floors on both arms. Pre-pin: the whole feature is at the pin (`kv_cache_coordinator.py:583,653`). |
| 171 | `8efa13b700` | 52401 | NA surface-absent | no | No runner selection to key on: `include/vllm/v1/worker/gpu/runner.h:9-33` is hard-committed to MRV2, and `use_v2_model_runner` occurs 3 times, all in comments. No PIECEWISE arm (`src/vllm/v1/worker/gpu/cudagraph_dispatch.h:256-258`). The deleted constant was itself added in-range by `87668ab69b`. |
| 172 | `41f12a0daf` | 52394 | NA surface-absent | no | None of the four validators exists. `backend_xgrammar.cpp` is 110 lines with no validation entry point; `ResolveStructuredOutputBackend` (`:79-93`) is a pure string map with no try-validate-then-fall-back, so the `except` narrowing has nothing to narrow. Pre-pin. |
| 173 | `84530eb235` | 52441 | NA surface-absent | no | No `MultiModalFieldConfig` abstraction, so no per-field device-placement flag: `keep_on_cpu`, `video_frame_counts`, `MultiModalFieldConfig`, `flat_from_sizes` → 0 (control `mm_hash` → 101). Gemma-4 has no video arm here at all. |
| 174 | `4d2a68d64d` | 52436 | NA surface-absent | no | This tree ported `structured_output/utils.py`, not the `gpu/structured_outputs.py` worker the commit edits (`src/vllm/v1/worker/gpu/runner.cpp:250-252`). `mask_stride`, `adaptive_verification` → 0, so the defect has no precondition. The sole call site passes an empty map (`runner.cpp:2931`). |
| 175 | `1f0e0bf612` | 52050 | NA surface-absent | no | No FA-version resolution function to edit. `include/vllm/v1/attention/backend.h:444-451` states the tree ships FA2 only and that the FA4 arms are "deliberately not written as always-false branches". Not `inert` per rule 5: there is no gate, there is no function. Deferred owner `BACKEND-CUDA-COMP-FA` (`.agents/backend-matrix.md:215`). |
| 176 | `eee538d5da` | 52482 | **REAL_GAP** | yes | `GetFreedMmHashes` (`src/vllm/v1/core/encoder_cache_manager.cpp:117-121`) is unfiltered; evict (`:69-72`) and re-insert (`:78`) both happen inside one `Schedule()`, reported at `scheduler.cpp:1074-1075`. A live worker tensor can be dropped. [#2686](https://github.com/mudler/vllm.cpp/issues/2686). |
| 177 | `967e104fad` | 52550 | NA surface-absent | no | There is no `AttentionConfig`; `ls include/vllm/config` has no such header. The indexer dtype is hard-coded (`deepseek_v4_registry.cpp:395-402`, head_size 132, pinned by a test) and the MXFP4 arm is already named as owed. Both knobs existed at the pin (`config/attention.py:64,67`). |
| 178 | `a6a2a93f9b` | 52528 | **ALREADY_SATISFIED** | no | The failure mode cannot occur: the port reads bodies only through `nlohmann::json::find`, which returns `end()` for every non-object (`third_party/nlohmann/json.hpp:22722-22732`). Every before-validator uses it (`src/vllm/entrypoints/openai/protocol.cpp:69-96`, `:180-196`, `:373-375`, `:551-563`); the embeddings handler is explicit (`api_server.cpp:526-528`). |
| 179 | `292187dd8c` | 52492 | NA surface-absent | no | The shortcut the commit guards does not exist: both indexer arms score unconditionally (`deepseek_v4.cpp:1069-1104`, `:1157-1176`; `deepseek_v4_dsa.cpp:546-573` sorts even when `take == avail`). Nothing to capture it into either — the indexer refuses inside the decode graph (`deepseek_v4.cpp:3567-3568`). Pre-pin (`attention.py:814`). |
| 180 | `a02cfccbc6` | 50729 | NA surface-absent | no | No block-level Mamba state copier: `token_bias`, `_copy_mamba_state_block`, `batch_memcpy` → 0 (control `conv_state` → 699 across 20+ files). The only `mamba_cache_mode` producer passes `"none"` (`qwen3_5_common.cpp:89`), so `"align"` is never selected. Owned by `KV-MAMBA-ALIGN` (`.agents/engine-matrix.md:101`). |
| 181 | `f27ae25473` | 51852 | NA surface-absent | no | Only the capability surface of `cpu_attn.py` was ported, and the header says so (`include/vllm/v1/attention/backends/cpu_attn.h:1-2`). `src/vllm/v1/attention/backends/cpu_attn.cpp` is 39 lines implementing one method. No CPU metadata builder, no scheduler-metadata scratchpad to mis-size. |
| 182 | `017e9f4448` | 52216 | **REAL_GAP** | yes | The consumer is written and reached (`single_type_kv_cache_manager.cpp:503-508`, `:523-530`, `:704-715`); the producer is missing and the value is hard-wired `nullopt` (`kv_cache_coordinator.cpp:166`, `.h:185`). Subsumes a pre-pin env read. Default flips `None` → `0`. [#2692](https://github.com/mudler/vllm.cpp/issues/2692). |
| 183 | `7075ddac28` | 52197 | **ALREADY_SATISFIED** | no | Both hunks are here and mirrored beyond-pin by an argued decision: `include/vllm/config/speculative.h:229-231` (hunk 1) and `:286-288` (hunk 2), each naming this SHA. Reached from `model_loader.cpp:1587,1597,1626`. Landed as `10fe7f475` under row `SPEC-DSPARK-QWEN3-ROUTING` (`.agents/engine-matrix.md:205`). See §6.3. |
| 184 | `cfbc5afbf7` | 52552 | NA surface-absent | no | No `RoutedExperts`, no shared expert-param mapping, no `lora_base_layer` prefix (all → 0; control `shared_experts` → 130). Expert names are built per model by direct concatenation (`kimi_k3_weights.cpp:281`), so there is no prefix composition to get wrong. LoRA-over-MoE is a named deferral (`include/vllm/lora/layers.h:48`, "W7"). Pre-pin. |
| 185 | `d1e3eee6fb` | 52188 | NA **inert:DCP** | no | DCP is inert-wired and hard-off: `src/vllm/v1/core/sched/scheduler.cpp:280` passes a literal `1`, and no CLI reaches it (`decode-context-parallel` → 0). ~10 lines have a live surface (the backend-validity list, `include/vllm/v1/attention/backend.h:340-390`); the rest is surface-absent. Owner `.agents/feature-matrix.md:140`. Also self-cancelling — §5.2. |
| 186 | `f08a95f8d8` | 52031 | NA surface-absent | no | There is no `EngineCoreReadyResponse` and no engine-to-frontend ready message: the boundary is in-process (`include/vllm/v1/engine/core_proc.h:20-26`, ZMQ handshakes listed DEFERRED at `:45`). Not `inert` — the struct does not exist. The whole message is pre-pin-absent. |
| 187 | `0db502c8d8` | 50493 | **REAL_GAP** | yes (1 of 3) | Part (b): `BlockTable::append_row` (`src/vllm/v1/worker/gpu/block_table.cpp:75-89`) writes `block_table_cpu_[base + start + i]` with no bound against `max_num_blocks_per_req`. Parts (a) and (c) are absent, (c) pre-pin. [#2687](https://github.com/mudler/vllm.cpp/issues/2687). |
| 188 | `cdb8545a91` | 52539 | NA surface-absent | no | No fused GDN decode MTP kernel: every `fused_gdn` hit is the *prefill* fusion. And no 8:1 rule to widen — this tree's GDN is already ratio-general (`src/vt/vulkan/vulkan_ops.cpp:1633`). The whole feature is **post-pin** (`1be3628367`), so the widening would land on nothing. §5.1. |
| 189 | `d5f5de7a7d` | 52626 | NA surface-absent | no | Neither the derived broadcast buffer nor any weight-sync path exists (`hc_attn_fn_broadcast`, `finalize_mhc` → 0; the whole `weight_sync|update_weights|sync_weights` family → 0). The two mHC sites read the full `hc_attn_fn` (`deepseek_v4.cpp:2462-2467`, `:2704-2712`). Pre-pin (`nvidia/model.py:831,1309`). |
| 190 | `aa9903490c` | 50156 | **REAL_GAP** | yes (1 of 4) | (a) The norm is hard-wired to Cohere LayerNorm with no `rms_norm_eps` selection **and no refusal** (`commandr.cpp:52-57,159,209`; `commandr_registry.cpp:108-111`) — silent wrong numerics. (b) `head_dim` already satisfied (`hf_config.cpp:448-452`). (c)/(d) surface-absent. [#2698](https://github.com/mudler/vllm.cpp/issues/2698). |
| 191 | `5c9ff5366b` | 46175 | **REAL_GAP** | yes | This tree rejects `-1` at `src/vllm/entrypoints/openai/protocol.cpp:372-375`, deliberately, and the comment at `:366-372` argues for it. Upstream has moved to the other side. The rest of the stack is already `-1`-safe (`sampling_params.cpp:121-123`, `input_batch.cpp:301`). [#2690](https://github.com/mudler/vllm.cpp/issues/2690). |
| 192 | `d29dc3ab87` | 52430 | NA surface-absent | no | The default flipped does not exist here: `Gemma4Parser` takes no kwargs (`include/vllm/parser/gemma4.h:27-28`), has no `_thinking_enabled` and no `is_reasoning_end` override — its sole upstream consumer. Deliberate and recorded (`src/vllm/parser/parser_manager.cpp:65-70`). Pre-pin. |
| 193 | `88b2bff2c6` | 51695 | NA surface-absent | no | No `FusedMoEFactory`, `determine_expert_counts`, `fuse_shared_experts` or `resolve_layer_fused_shared_expert` (all → 0). Shared experts always run as a separate dense MLP (`qwen3_5.cpp:7000`, `:7279-7284`). The concept exists once, as a stub returning False in the tree's own oracle harness (`tools/parity/dump_moe.py:187-189`). Would be `inert:ROCm-AITER` if the factory landed. |
| 194 | `bca7bea240` | 52182 | NA surface-absent | no | No kernel-selection registry to reorder (`_POSSIBLE_FP8_KERNELS`, `MarlinFP8ScaledMMLinearKernel` → 0). The tree hard-selects one kernel per arm and cites upstream's list only as prose. The winner on sm_121 is CUTLASS before and after. **But it has a live record consequence** — §6.4. |
| 195 | `01e56caaf2` | 52512 | NA surface-absent | no | No per-layer prefill-backend object to gate; this tree models a different, *per-step* predicate (`include/vllm/model_executor/models/mla_attention.h:594-640`, "upstream's `use_dense_mha` is a property of the step and not of the layer"). Entirely **post-pin**: the flag is absent at `5559679229` and so is the file, and this is the middle of a three-commit lineage. §5.1. |
| 196 | `6948a43fbb` | 52161 | NA surface-absent | no | `is_hybrid` here is a compile-time literal per registration (`include/vllm/model_executor/models/model_registry.h:68`) with exactly one reader (`model_loader.cpp:1355`) and zero non-literal assignments. `layer_types` is carried but never feeds it. The `layer_types` override was **already at the pin** — §6.2. |
| 197 | `12f64b39d2` | 52805 | **REAL_GAP** | yes | `NativeGrammar::accept_tokens` (`backend_native.cpp:1139-1147`) has no post-termination early-out and returns `false` in the done state (`:1210-1212`); the multi-token caller is unguarded and fails the request (`scheduler.cpp:1286`). Worse than the bug upstream fixed. [#2691](https://github.com/mudler/vllm.cpp/issues/2691). |
| 198 | `ef47a897e2` | 51875 | **ALREADY_SATISFIED** | no | Determinism has been the default here since before upstream adopted it: `init_none_hash` (`src/vllm/v1/core/kv_cache_utils.cpp:376-412`) falls back to `kDefaultNoneHashSeed`, the deviation is recorded at `include/vllm/v1/core/kv_cache_utils.h:216-237`, and it is reached from production (`model_loader.cpp:1392-1405`, `:2119`). §6.1 corrects two records that said otherwise. |
| 199 | `a9f4afb66f` | 51368 | NA surface-absent | no | No MegaMoE, so no per-forward finalize to remove: it is explicitly out of scope as SM100-only (`include/vllm/model_executor/models/deepseek_v4_moe.h:36-38`). All weight finalization is load-time (`deepseek_v2.h:402-405`). The mHC half lands on nothing for `[189]`'s reason. Pre-pin. |
| 200 | `f1178f3a06` | 52836 | NA surface-absent | no | A revert of something never ported: `eager_scratch_pool` → 0 (the `ScratchPool` symbols present are unrelated FA2/GDN per-stream pools). And structurally non-work — its paired add is also in this queue. §5.2. |

## 4. The ten real gaps

### 4.1 The table

| # | upstream | what is missing | rough size | owning row | issue |
|---:|---|---|---|---|---|
| 162 | `51def7848a` | the DSpark k-floor refusal must be deleted, plus an unmade decision on the widened fallback | ~15 lines product, 100-250 with tests and records | `SPEC-DSPARK-BLOCK-SIZE-GUARD` (`.agents/engine-matrix.md:206`) | [#2657](https://github.com/mudler/vllm.cpp/issues/2657) |
| 163 | `38f097fab8` | a NUL guard in `StructuredOutputsParams::Verify()` | ~6 lines + a test | `TOOLS-STRUCTURED-CORE` (`.agents/engine-matrix.md:160`) | [#2701](https://github.com/mudler/vllm.cpp/issues/2701) |
| 164 | `b216db3ed0` | the whole out-of-vocab check, both bounds, on three entry points | ~20-30 lines + vocab-size plumbing | **none exists** (recorded at `.agents/porting-inventory.md:52`) | [#2699](https://github.com/mudler/vllm.cpp/issues/2699) |
| 167 | `57bd0ed441` | `head_size_v` hoist, `num_head_slots`/`state_content_bytes`, one `unpadded_page_size_bytes`, `customize_spec` **and its call site** | ~250-400 lines | **none exists** for the series | [#2695](https://github.com/mudler/vllm.cpp/issues/2695) |
| 176 | `eee538d5da` | filter same-pass re-allocated hashes out of `GetFreedMmHashes` | ~4 lines + ~30 test | `ENG-MM-INPUT-PIPELINE` (`.agents/engine-matrix.md:77`) | [#2686](https://github.com/mudler/vllm.cpp/issues/2686) |
| 182 | `017e9f4448` | `prefix_cache_retention_interval` producer, default `0`, validator, ABI knob | ~80-140 lines + tests | `KV-MAMBA-ALIGN` (`.agents/engine-matrix.md:101`) | [#2692](https://github.com/mudler/vllm.cpp/issues/2692) |
| 187 | `0db502c8d8` | a row-capacity check in `BlockTable::append_row` | ~8 lines + a red-first test | `ENG-RUNNER-MODELSHAPE` (`.agents/engine-matrix.md:76`) | [#2687](https://github.com/mudler/vllm.cpp/issues/2687) |
| 190 | `aa9903490c` | RMSNorm-vs-LayerNorm selection by `rms_norm_eps`, or a refusal by name | ~40-60 lines, or ~15 for the refusal | `MODEL-TEXT-commandr-cohere-for-causal-lm` (`.agents/model-matrix.md:216`) | [#2698](https://github.com/mudler/vllm.cpp/issues/2698) |
| 191 | `5c9ff5366b` | accept `logprobs=-1` on `/v1/completions`, plus two record edits | ~5 lines + re-point 2 tests | `SAMPLE-LOGPROBS` (`.agents/engine-matrix.md:142`) | [#2690](https://github.com/mudler/vllm.cpp/issues/2690) |
| 197 | `12f64b39d2` | stop advancing the grammar once the head snapshot is done | ~15 lines + a red-first test | `TOOLS-STRUCTURED-CORE` (`.agents/engine-matrix.md:160`), record edit on `TOOLS-XGRAMMAR` (`.agents/engine-matrix.md:161`) | [#2691](https://github.com/mudler/vllm.cpp/issues/2691) |

Every owning row named above was re-resolved by line number in
`.agents/engine-matrix.md` or `.agents/model-matrix.md` before its issue was
filed. **Two gaps have no owning row and their issues say so in as many words**
rather than naming a row that would pass `check-agent-record.py` without
resolving.

### 4.2 Three of the ten are worse than a missing forward-port

- **`[187]`** is a silent out-of-bounds write, not a missing feature. An
  over-long row writes into the next row of a `std::vector<int32_t>`, or past the
  end on the last row. The pressure is real — `MambaManager` allocates
  `cdiv(num_tokens, block_size) + num_speculative_blocks`
  (`single_type_kv_cache_manager.cpp:798`) with a live non-zero producer
  (`qwen3_5_common.cpp:90`), against a row width of `cdiv(max_model_len, bs)`.
  What keeps it in bounds today is arithmetic luck: the width is rounded up to a
  multiple of `128 / block_size`, granting up to 7 blocks of slack at
  `block_size == 16`. Nothing was run, so whether a shipped configuration crosses
  it is undetermined; a guard is warranted either way.
- **`[197]`** is worse in this tree than the bug upstream fixed. Upstream's
  terminated matcher merely returned `False`; here the unguarded scheduler caller
  turns that into `RequestStatus::kFinishedError` (`scheduler.cpp:1286`), so a
  batch whose grammar terminates before its last token **kills its own request**.
  The issue carries a caution the implementer needs: `is_terminated()` is also
  true for a lazy grammar awaiting its trigger, so an unconditional early-out
  would silently disable constraint enforcement for every structural-tag grammar.
- **`[190]`** produces silently wrong numerics rather than a refusal. A Cohere
  config carrying `rms_norm_eps` runs mean-centred LayerNorm at a 1e-5 default.
  The issue names the cheaper honest option — refuse by name, the tree's own
  idiom at `commandr_registry.cpp:131-138` — because the row is `BLOCKED` with no
  runnable vehicle and a refusal beats wrong output.

## 5. Three entries are structurally non-work, and that is a queue defect

These are not judgements about this tree. They would be non-work against any
tree, and no amount of reading `src/` could have found them — only the pin check
and the pairing check did.

### 5.1 Two entries edit a surface that does not exist at the pin

`[188]` widens the eligibility of a fused GDN decode-MTP kernel. The **entire
kernel** arrived after the pin: `git show 5559679229:csrc/.../fused_gdn_decode_kernel.cu`
does not exist, and `git log --diff-filter=A` names one commit, `1be3628367`
vllm#51674, inside the range. Porting the widening without the parent lands on
nothing.

`[195]` is the middle of a three-commit lineage. `supports_dense_mha_prefill` is
absent from `mla_attention.py` at the pin, and `vllm/models/deepseek_v32/attention.py` —
the commit's second file — does not exist at the pin either. `git log -S` gives
`58aa1e3d26` → `01e56caaf2` → `de9250ac9e`, all post-pin.

### 5.2 Two self-cancelling add/revert pairs sit inside the range

`[200]` `f1178f3a06` reverts `df71917cf1`, which is **also in this queue**, at
entry 45. Hashing pin-vs-target for every path the revert touches leaves
`eager_scratch.py`, the fused qnorm/rope/kv-insert kernel and
`sparse_attn_compress_cutedsl.py` byte-identical: **the net upstream delta across
the range is zero.** PORTQ-2 labelled entry 45 `surface-absent` on tree evidence
alone and could not see the pairing, because a tranche sees forty entries.

`[185]`'s refusal is the same shape: `aeeb36b1f1` vllm#50000 adds the
DSpark-plus-DCP refusal that `d1e3eee6fb` deletes, both inside the range. Here
only the delete is in the 315.

This is the C9 hazard `.agents/sync/2026-09-02-db92053.md:135` records for the
DSpark block-size chain, twice more and unrecorded. **`2026-09-01-cdefd9d.md` §4
is annotated in place for the `f1178f3a06`/`df71917cf1` pair** (§6.5), because a
wave meeting only one half would read a phantom delta.

### 5.3 One entry is portable but must not land alone

`[167]` is stage 5 of the KV-cache layout refactor. `.agents/sync/2026-09-02-db92053.md:127-144`
records that stages 4 and 5 are inside the range and **stage 6 (`8bdc70ec7b`) is
not**, so porting 4 and 5 without 6 lands a half-migrated layout surface no
upstream revision ever had. The label stays `REAL_GAP` — portable work
demonstrably exists — but the scheduling constraint is on the issue, along with
two preparatory steps the earlier sync records already name.

## 6. What re-verification of the five prior readings added

`git log --grep` on all forty SHAs and all forty PR numbers returned **nothing**:
no commit in this repository triages any of these forty. Five entries had a prior
reading in `.agents/`, and all five are reconciled here.

### 6.1 `[198]` contradicts two records, and both are corrected in place

`.agents/sync/2026-09-01-adebc41.md:946` lists `ef47a897e2` among the items that
"make the prefix-cache `NONE_HASH` deterministic by default **and therefore
change block hash values**", and `.agents/sync/2026-09-01-cdefd9d.md` §4 ends its
entry with the same clause.

**The clause is false about this tree.** Block hashes here are already
deterministic by default and have been since before this commit landed upstream:
`init_none_hash` resolves explicit arg → `$VLLM_PREFIX_CACHING_HASH_SEED` →
`$PYTHONHASHSEED` → `kDefaultNoneHashSeed`
(`src/vllm/v1/core/kv_cache_utils.cpp:376-412`), and the header states the
deviation deliberately (`include/vllm/v1/core/kv_cache_utils.h:216-237`:
"our threat model has no hash-DoS concern, so the deterministic value is the
DEFAULT and randomness is the opt-in"). Upstream's new `get_none_hash_seed()` is
answered by `none_hash_provenance()` (`:207-214`), and the whole thing is reached
from production at `model_loader.cpp:1392-1405`, called from the member-init list
at `:2119`. **At the pin, upstream is the side that is behind this tree.**

Both records are annotated rather than rewritten, so a fifth wave does not queue
a no-op described as changing block hash values. Two residuals are recorded with
them and neither is this commit's: the seed literal differs from upstream's
(detected, not silently mis-hit, by `src/vllm/v1/kv_offload/cache_identity.cpp:49,107-108`),
and upstream's `xxhash` branch has no surface because `sha256_cbor` is the only
shipped hasher.

The third record, `.agents/sync/2026-09-02-db92053.md:133`, is **not** corrected:
its table asserts only that the commit is inside the range, which is true.

### 6.2 `[196]`'s real distance is a pre-pin override, and the first probe read it backwards

`ModelConfig.is_hybrid` at the pin already carries the `layer_types` override the
commit edits (`git show 5559679229:vllm/config/model.py:1729-1737`). This tree
has neither half of it: `is_hybrid` is a compile-time literal on each
registration (`include/vllm/model_executor/models/model_registry.h:68`), and
`grep -rn '\.is_hybrid' src include` minus the literal initialisers returns
**exactly one** line — `model_loader.cpp:1355`, the sole reader, inside
`ResolveEnablePrefixCaching`. `layer_types` *is* carried
(`include/vllm/transformers_utils/hf_config.h:106`) and simply never feeds a
hybridness decision.

Worth recording as method: an operator probe that omitted `-r` suggested the tree
has no `is_hybrid` at all, which is false and would have been the wrong
`surface-absent` for the wrong reason. The `-r` probe found 45 hits, and the true
answer — the *derivation* is absent while the flag is present — is narrower than
either. This is the fifth mechanism of §2.3 producing a plausible wrong answer
rather than an obvious one.

### 6.3 `[183]` is the wave's clearest ALREADY_SATISFIED, and the row is `ACTIVE` for evidence, not code

Both hunks of vllm#52197 are here, mirrored **beyond the pin** by an argued
decision and each naming this SHA in a comment
(`include/vllm/config/speculative.h:229-231` and `:286-288`), reached from
production at `model_loader.cpp:1587,1597,1626`. The work landed as `10fe7f475`
under `SPEC-DSPARK-QWEN3-ROUTING` (`.agents/engine-matrix.md:205`), with 12+7
committed cases. The row sits `ACTIVE` rather than `DONE` because two run gates
are GPU-blocked — **its evidence is owed, not its code**, and this wave executed
nothing so it cannot discharge them.

### 6.4 `[194]` has no code consequence and a live record consequence

The commit deletes `VLLM_TEST_FORCE_FP8_MARLIN` and the `cc >= 89` Marlin
exclusion. This tree has no priority list to reorder, and the winner on sm_121 is
CUTLASS before and after — so no behaviour moves.

What does move is four prose sites that assert both as current upstream fact
(`src/vt/cuda/fp8_per_tensor_dispatch.h:33-36`, `include/vt/ops.h:2966-2969`,
`src/vt/cuda/cuda_matmul_fp8_block_cutlass.cu:23-28`,
`src/vt/cuda/cuda_matmul.cu:700-703`), plus the Laguna oracle recipe recorded as
`VLLM_TEST_FORCE_FP8_MARLIN=1` (`src/vllm/model_executor/models/laguna.cpp:440`,
`.agents/benchmark-record.md:2401,2411`,
`.agents/specs/laguna-nvfp4-arm-2026-07-31.md:295`) — an env var that will not
exist at the target, making that lower bound unreproducible until re-expressed.

This is the `ENG-RECORD-ANCHOR-RATCHET` shape (`.agents/engine-matrix.md:231`):
the cited lines still exist and hold plausible neighbouring code, so a reader who
checks the anchor is **confirmed in a wrong belief**. It is recorded here and not
filed as a gap, because no product behaviour is missing and **this cycle does not
advance the pin** — the debt falls due when it does.

### 6.5 `[200]`'s pairing is written back into the queue

`.agents/sync/2026-09-01-cdefd9d.md` §4 is annotated on both `f1178f3a06` and, by
reference, `df71917cf1`. The annotation adds a dated correction line and leaves
the original text intact, which is the shape that file already uses. **Verified
that the annotation does not perturb the SHA extraction: §4 still yields exactly
315.** (Its line span moved from 205-563 to 205-566, so a hard-coded `sed` range
would silently over-read into §5 and count 343 — derive the span from the
headings.)

## 7. The one `inert` entry, and why the call was close

`[185]` `d1e3eee6fb` is labelled `inert:DCP`. DCP plumbing exists and is
hard-wired off: `src/vllm/v1/core/sched/scheduler.cpp:280` passes a literal `1`,
`include/vllm/v1/core/kv_cache_manager.h:145` defaults it to `1`, and no command
line reaches it. So no configuration this tree builds can set DCP > 1 — that is
the gate.

The call is close because **only about ten lines of the commit have a surface
here at all** (adding `use_dcp` and `supports_non_causal_dcp` to the
backend-validity list at `include/vllm/v1/attention/backend.h:340-390`, which
would be dead on arrival). Everything else — the ClassVars, the metadata builder,
the FlashInfer MLA backend, the DCP local sequence lengths in the speculator — is
`surface-absent`, and the refusal the commit *deletes* was never ported. A reader
weighting by volume would have written `surface-absent`. Either label defers it
to the same owner: `.agents/feature-matrix.md:140` (Context parallel PCP + DCP,
`☐ T2 (scoped)`, spec `specs/parallelism-modes.md`), whose own note —
"latent scaffolding inert-wired (`block_table.cpp:30-32`)" — is exactly what was
found. **No new issue was filed**, because that row already owns the gate and one
more issue saying "do not port this yet" is the intake-without-exit shape
AGENTS.md warns about. Note for whoever takes it: port the **post**-`d1e3eee6fb`
state, since the pre-state refusal it deletes was never here.

## 8. What each `NOT_APPLICABLE` resting on absence rests on

Every row below is a zero paired with a positive control run through the
identical probe form, plus the scope of the zero. Scope `S+I` is `src/`+`include/`;
`S+I+T` adds `tests/`. Every probe carries `-r`.

| # | probe (zero) | scope | positive control, same form |
|---:|---|---|---|
| 161 | `timed_trace`, `TimedTrace`, `SampleRequest` | S+I+T+tools+benchmarks+examples+scripts+docs | `ShareGPT` 5, `kv_cache_dtype` 262, `trace` 1889 |
| 165 | `use_attn_reduce_scatter_for_moe`, `reduce_scatter`, `ReduceScatter`, `use_sequence_parallel_moe` | S+I+T | `AllGather` → 5 files incl. `src/vt/cuda/nccl_communicator.cu`; `tensor_parallel` → 5 files |
| 166 | `paged_mqa_logits`, `PagedMqaLogits`, `use_flattening`, `native_next_n`, `scheduler_metadata` | S+I+T | `DsaTopkSelect` 78 |
| 168 | `override_attention_dtype`, `override-attention-dtype`, `OverrideAttentionDtype`, `attention_dtype` | S+I+T+tools+benchmarks+examples+scripts+docs | `kv_cache_dtype` 262 |
| 169 | `decode_swa_width`, `decode_swa_indices`, `noncausal_index_width`, `get_dspark_swa_index_width`, `WorkspaceManager` | S+I and T | `sparse_swa.py` 15/14, `IndexerSelectCompressed` 3/5, `hc_attn_fn` 18/110 |
| 170 | `enable_partial_hash_hits`, `partial_hash_hits`, `num_reprefillable_tokens`, `_align_cacheable`, `AlignCacheable` | S+I+T | `scheduler_block_size` 71, `use_eagle` 58, `retention_interval` 26 |
| 171 | `MRV1_UNSUPPORTED`; `use_v2_model_runner` 3 hits, all prose | S+I | `piecewise|cudagraph_mode` 20+ |
| 172 | `Invalid grammar specification`, `Failed to transform`, `ValidateXgrammar`, `validate_structured_output`, `structural_tag specification` | S+I+T | `xgrammar` 128, `compile_grammar` 34, `StructuredOutputsParams` 38 |
| 173 | `keep_on_cpu`, `video_frame_counts`, `MultiModalFieldConfig`, `flat_from_sizes` | S+I+T | `mm_hash` 101; `video` in `inputs.h` → :37,:38,:44 |
| 174 | `mask_stride`, `adaptive_verification`, `AdaptiveVerification` | S+I | `grammar_bitmask` 10 files, `num_draft_tokens_per_req` 5 files |
| 175 | `GetFlashAttnVersion`, `requires_local_attention`, `RequiresLocalAttention`, `FaVersion` | S+I+T | `flash_attn_version`/`fa_version` → 3 comment hits stating the FA2-only fact |
| 177 | `indexer_kv_dtype`, `IndexerKVDType`, `resolve_indexer_kv_dtype`, `AttentionConfig` | S+I and T | `hc_attn_fn` 18/110, `sparse_swa.py` 15/14 |
| 179 | `is_current_stream_capturing` (model path) | S+I | `cudaStreamIsCapturing` 8 in `src/` — the concept IS greppable here |
| 180 | `token_bias`, `_copy_mamba_state_block`, `batch_memcpy`, `state_block_stride` | S+I+T | `conv_state` 699 across 20+ files, `mamba_cache_mode` 19 |
| 181 | `scheduler_metadata`, `get_num_attention_heads_from_layers`, `CPUAttentionMetadataBuilder`, `_group_sliding_window`, `attn_isa` | S+I | `CommonAttentionMetadata` 401, `is_hybrid` 45, `layer_types` 305 |
| 184 | `lora_base_layer`, `base_layer`, `RoutedExperts`, `routed_experts_prefix`, `ExpertParamsMapping` | S+I | `shared_experts` 130, `CommonAttentionMetadata` 401 |
| 185 | `supports_non_causal_multi_token_dcp`, `supports_dcp_with_varlen`; `dcp` in `*/spec_decode` | S+I+T; the two spec_decode dirs | `dcp_world_size` → 6 files, `seq_lens` → 3 hits in spec_decode, `tensor-parallel` 3 |
| 186 | `ReadyResponse`, `supports_lora`, `instance_id` | S+I+T | `EngineCoreOutputs` 83, `InprocClient` 31, `max_model_len` 615 |
| 188 | `fused_gdn_decode`, `decode_post_conv`, `MTP_TOKENS`, `gdn_decode_kernel`; `8 * num_k_heads` | S+I(+T) | `GdnDecodeKernel` → 8 files, `GdnPostConv` → 10, `num_v_heads` → 3 |
| 189 | `hc_attn_fn_broadcast`, `finalize_mhc`, `weight_sync|update_weights|sync_weights` | S+I and T | `hc_attn_fn` 18/110, `process_weights_after_loading` 15/2 |
| 192 | `thinking_enabled` in any gemma4 file | S+I+T | `thinking_enabled` → 5 hits in `parser/qwen3.cpp` and `parser/qwen3.h` — the same probe finds the concept where the tree carries it |
| 193 | `FusedMoEFactory`, `determine_expert_counts`, `fuse_shared_experts`, `resolve_layer_fused_shared_expert`, `quark` | S+I | `shared_experts` 130, `MoeGroupedGemm` 155, `MergedGemmGroup` 19 |
| 194 | `_POSSIBLE_FP8_KERNELS`, `MarlinFP8ScaledMMLinearKernel`, `HummingFP8`, `FlashInferFP8` | S+I | `kv_cache_dtype` 127, `MoeGroupedGemm` 155; `Humming` 1 and `VLLM_TEST_FORCE_FP8_MARLIN` 1, both prose — which is §6.4's point |
| 195 | `supports_dense_mha_prefill`, `MLAPrefillBackend`, `short_prefill`, `scoring_skip` | S+I+T | `dense_mha` 15, all the per-step rule; `GLM-5.3` present where `GLM-5.2` is 0 |
| 196 | `.is_hybrid` minus literal initialisers → exactly 1 | S+I | `is_hybrid` 45, `layer_types` 305, `full_attention` 98, `granite` 152 |
| 199 | `finalize_mega_moe`, `finalize_mhc`, `hc_attn_fn_broadcast`, `transformed_l1` | S+I and T | `hc_attn_fn` 18/110, `DeepseekV4CompressorState` 11/3 |
| 200 | `eager_scratch_pool`; the six fused-kernel names | S+I and T | `EagerScratch|scratch_pool|ScratchPool` 10 (FA2/GDN pools), `scratch` in `deepseek_v4.cpp` 16 |

`[176]`, `[178]`, `[183]`, `[191]`, `[197]` and `[198]` carry no absence claim —
each rests on printed present code.

## 9. What could not be determined

- **Whether several issue numbers cited across `.agents/` still resolve.**
  `#100`, `#249`, `#1225` and `#2286` return 404 through `gh api` while `#4`,
  `#2611`, `#2647` and `#2657` resolve normally, and `gh api user` reports the
  token is not suspended. A scattered 404 pattern with healthy neighbours is
  exactly the shape that once produced a false "817 issues were deleted" claim in
  this repository's own records, so **no conclusion is drawn**: this is
  `REMOTE_UNVERIFIED`, not evidence of dangling references. It matters because
  `.agents/specs/logprobs-all-sentinel.md` names one of them as the owner of
  `[191]`'s divergence, and `.agents/specs/dspark-qwen3-routing.md` names another.
- **Reachability of three real gaps.** `[187]`'s bound may hold by arithmetic
  slack; `[197]`'s post-EOS batch may not arise in a shipped configuration;
  `[176]` needs two requests sharing an mm_hash under cache pressure in one step.
  Nothing was run, so each is recorded on its issue as unestablished. The mirror
  defect is present in all three regardless.
- **`[163]`'s trigger.** No shipped entry point can currently deliver an embedded
  NUL — the C ABI carries a NUL-terminated `const char*` and the HTTP path never
  populates `regex`. Whether any in-tree caller sets it from untrusted bytes is
  unknown.
- **`[190]`'s liveness.** Whether any published Cohere checkpoint ships
  `rms_norm_eps`. No checkpoint was fetched.
- **`[167]`'s design question**, carried forward from
  `2026-09-01-cdefd9d.md:1901-1907` and not resolved: whether `state_content_bytes`
  is the right home for `kFp8DsMlaV4TokenBytes`/`kFp8DsMlaV32TokenBytes`. It needs
  reading how upstream publishes those bytes *after* stage 5.
- **`[182]`'s blast radius.** Which shipped gate models exercise a SlidingWindow
  or Mamba group with prefix caching on, which is what the `None` → `0` default
  flip changes.

## 10. An estimate, labelled as one

**This is an extrapolation from a contiguous, non-random slice, not a
measurement.** Four tranches now read 160 of 290 entries and found
6 + 9 + 8 + 10 = **33 real gaps**, a rate of 20.6%. Applied to 290 that projects
to roughly **60 gaps**, consistent with #2524's stratified-sample estimate of ~55
from 63 entries.

Four reasons not to treat it as a count, and this tranche adds a fifth:

1. The slices are contiguous, so subsystem clustering is not averaged out. Four
   of this tranche's ten sit in two subsystems (structured output, KV cache).
2. Two entries landing either way moves this tranche from 20% to 25% or 15%.
3. The tranches share worked entries with #2524's population.
4. `REAL_GAP` counts entries, not effort. This tranche's ten range from ~4 lines
   (`[176]`) to ~250-400 (`[167]`), so a gap count is not a work estimate.
5. **New here: some entries are non-work for reasons independent of the tree.**
   Three of these forty (§5) would be non-work against any tree. If that rate
   holds, roughly 20 of the 290 are queue artefacts rather than candidates, and
   the denominator itself is soft.

## 11. What the next tranche should do, in cost order

1. **`git log --grep` on the SHA and the PR number.** Cheapest, and it came back
   empty for all forty here — which is itself worth knowing in one command.
2. **`git show 5559679229:<upstream path>`.** The highest-yield check in this
   wave: it settled or reframed nine entries, and it is the only thing that finds
   `[188]`'s and `[195]`'s post-pin parents.
3. **Ask whether the commit has a mate in the range.**
   `git log <pin>..<target> -- <path>` and a pin-vs-target hash of each touched
   file. Two self-cancelling pairs turned up here (§5.2) and a tranche cannot see
   them from inside its own forty.
4. **Shape B**: grep `.agents/*.md` and the `DEFERRED` comment blocks in
   `include/` before opening an upstream diff. It settled much of this tranche
   from deferral text already committed here.
5. **Probe hygiene, now six mechanisms deep.** Always `-r` on a directory
   argument, one needle per `-F` call, quote every `--include=*.h`, quote every
   path variable, and run the positive control through the identical form — the
   `$scope` failure was caught *only* because its controls went to zero too.
6. **Verify a derived order with `%ct`, never `%cd`.** One command
   (`git log -1 --format=%ct | sort -c -n`) checks the whole queue.
